# Log 2026-08

Part of [Log](./Log.md). Oldest first.

- 2026-08-20: **project notes moved from the Obsidian vault into this repo's `docs/`.** Root cause being fixed: the log lived in a different repo than the code, so a log entry could never be part of the commit it described - which is exactly how `2054eca` shipped unlogged and the hub kept claiming v1.1 was "not yet committed" weeks after it was live. The vault path `01-Projects\Tower Defense PWA\` is now a Windows directory junction (`mklink /J`) pointing at `docs/`, so Obsidian keeps the project in its graph and existing `[[Tower Defense PWA]]` links from other vault notes still resolve; the junction is git-ignored on the vault side so its git doesn't try to swallow this repo. Internal `[[wikilinks]]` became relative markdown links (they render on GitHub, where wikilinks don't); the two links out to vault-only notes became plain text. Repo's `CLAUDE.local.md` (which existed only to point sessions at the vault) deleted in favour of a committed `CLAUDE.md`. Stale status prose reconciled with git, and the missing `2054eca` entry backfilled into [Log 2026-07](./Log%202026-07.md).
- 2026-08-23: **docs move finished and committed** - the 08-20 session left the notes moved but untracked and the junction never made: `C:\vito\secondbrain\01-Projects\Tower Defense PWA` was a plain empty folder, so Obsidian had silently lost the project from its graph for three days. Ran `finish-junction.ps1` (it found the folder empty as expected, removed it, `mklink /J` to `docs/`) and deleted the script, as it was written to be deleted. Also found a stray `docs/.obsidian/` - the folder had been opened directly as its own vault - and git-ignored it rather than committing local editor state. `README.md` (written 08-20, also untracked) ships in the same commit.
- 2026-08-23: **v1.2 chosen - tower panel + upgrades.** Read `tower.ts` and `main.ts` before picking, same rule as v1.1: a placed tower today has no stats readout, no upgrade, no undo, and its only interaction is `overclockArmed` ("press Q, then click a tower"), which is a keyboard mode in a game that ships a touch toolbar. One selection model is the fix for all four, and [Idea Bank](../Idea%20Bank/Idea%20Bank.md) had already named the tap-tower panel the foundation under upgrades, sell, and touch Overclock. Specced in [Tower Panel](../Design/Tower%20Panel.md), six-step build order in [Roadmap](../Roadmap.md) (selection → panel shell → sell → tiers → Overclock migration → verify). Decisions worth keeping: panel is DOM like the v1 toolbar (native touch handling, the canvas hit-testing alternative bought nothing then either); selection and placement can share one `pointerdown` because `isPlaceable` already rejects `occupied` tiles, so the targets are disjoint; sell tracks `invested` on the entity instead of recomputing from the cost table, which would break the first time costs are rebalanced; tiers extend the per-kind stats table rather than adding a parallel system. Sell lands before upgrades on purpose - it is the smallest mutation that exercises the whole select-act-deselect loop.
- 2026-08-23: **v1.1's owed QA folded into v1.2's verify step** instead of being done first. Testing the multi-size/DPR and physical-device playthrough now would exercise the arm-then-click Overclock flow that v1.2 deletes, so the phone test happens once, against the interaction the game will actually ship. Recorded against the v1.1 roadmap item so it doesn't read as quietly dropped.
- 2026-08-23: also decided what v1.2 will *not* do - no tier sprites (the tier tell stays procedural; three sprites per tower would swallow the milestone and [Sprite Spec](../Design/Sprite%20Spec.md) has no tier variants), and no wave rebalancing (upgrades change the economy's shape, so tuning the four demo waves before that settles would be wasted twice).
- 2026-08-23: v1.2 step 1 done - **selection model.** `selectedTower` in `main.ts`, a new `pointerdown` branch that selects a tower on its tile and clears the selection on any other tap, Escape as the keyboard out, and `drawSelection` in `render.ts` (corner brackets on the tile plus the range circle in the tower's ring color). Brackets rather than a full tile outline so the 28px tower sprite is not boxed in by a border one pixel off its own silhouette. The selection branch sits *after* the `overclockArmed` branch for now so the old arm-then-click flow keeps working until step 5 deletes it. `tsc` clean; **not verified in browser this session** - the Chrome extension was not connected, so the visual check is owed.
- 2026-08-23: v1.2 step 2 done - **panel shell.** New `src/panel.ts` (`createTowerPanel`) rather than more code in `main.ts`, which was already the biggest file; the panel owns its DOM and exposes one `update(tower | null)`. Anchored top-right: the toolbar holds the bottom edge and the HUD text holds the top-left, so that corner was the only one free on a phone in landscape. Rows are built once and rewritten in place instead of re-rendering `innerHTML` each frame - a rebuilt subtree would destroy the buttons steps 3-5 put here, and any tap in flight on them. Aura towers show SLOW as the cut they apply (50%, not 0.5) with no damage/rate rows, since those numbers are meaningless for them. `tsc` clean; browser check still owed.
- 2026-08-23: v1.2 step 3 done - **sell.** `invested` on the `Tower` entity (set to the placement cost, and step 4 will add upgrade spend to it), `SELL_REFUND = 0.6`, `sellValue()` in `tower.ts`. Selling splices the tower, frees its tile in `occupied`, pays the refund, and clears the selection. The panel acts on the tower it is currently showing (a `current` captured by `update`) rather than reading a shared selection variable, so a button pressed after the selection moved cannot operate on a stale tower. Panel buttons reuse the toolbar's button rule in `style.css` instead of a second copy of the 44px-target styling. `tsc` clean; browser check still owed.
- 2026-08-23: v1.2 step 4 done - **upgrade tiers.** `TOWER_STATS` became `TOWER_DEFS` (name + placement + a 3-entry tier ladder per kind), flattened once at module load into the shape `towerStats(kind, tier = 1)` returns - the default keeps every existing call site correct, since all of them ask about placement, and flattening at load keeps the function allocation-free in a function that runs several times a frame. `applyUpgrade` writes the next tier's stats onto the entity and adds its cost to `invested`, so the sell refund follows upgrades for free. Attack towers buy damage + fire rate, auras buy slow + radius; Honeypot's radius climbs 0.6 to 0.8 only, because a wide Honeypot stops being a chokepoint and starts being a second IDS Scanner. Panel shows `TIER n/3` and the next tier's value in its own green column next to the current one. Tier tell on the board is procedural pips along the tile's bottom edge, per spec - no tier sprites this milestone. `tsc` clean; browser check still owed.
- 2026-08-23: v1.2 step 5 done - **Overclock is no longer a mode.** `overclockArmed` and the toolbar's OC button are gone; the panel carries an `OC` button that fires on the tower being shown, absent (not disabled) on aura towers, which have no Overclock to trigger. `Q` survives as a desktop shortcut but now acts on the selection, so the invisible "armed, waiting for a target" state that nothing on screen announced is gone with it. This is the actual mobile fix the milestone was chosen for: two taps with hidden state in between became one tap on a thing that is visibly selected. `tsc` clean; browser check still owed.
- 2026-08-23: v1.2 steps 1-5 built in one session, `tsc` and `vite build` clean, **and none of it has been seen running** - the Chrome extension was not connected, so every "verified in browser" this project usually writes is missing here. Step 6 stays open on purpose. Two things the read-through caught and fixed instead of a playthrough: the tier pips landed on top of the AES Turret's sprite (30px in a 32px tile leaves them nowhere clear), so they got a dark backing rect; and the panel was rewriting a dozen text nodes every frame off the render loop, so `update()` now bails on an unchanged signature - Cycles enter that signature clamped to the upgrade cost, since the only thing a balance above the price decides is nothing. Cosmetic deviation from the spec: the upgrade button reads `UP`, not `UPGRADE`, which did not fit next to `OC` and `SELL` at a 44px target on a phone.
- 2026-08-23: **still owed on v1.2** - the step 6 playthrough (place, select, upgrade to tier 3, sell, Overclock, restart), at several window sizes and DPRs, then on a physical phone. Specific things to distrust first, since they were reasoned about rather than watched: whether the tier pips read at all against each tower, whether the top-right panel collides with the HUD text or the notch in landscape, and whether tapping a tower on a touch screen selects it without the tap also being read as a placement.
- 2026-08-23: **build preview added to the panel** (user's call, mid-milestone, not one of the original six steps). Seeing the panel work made the omission obvious: the game was withholding what a tower does until after you had paid 20 to 45 Cycles to find out. The panel now runs two modes - selected tower, or the kind armed in the toolbar (cost, stats, placement rule) - and stays on screen for the whole game rather than only while something is selected. Build mode is also the only place the placement rule shows, which matters most for Honeypot: on-path only was previously discoverable only by trying it and getting an unexplained red tile. Two knock-on changes: rows went from 3 to 4 so the placement line fits under an attack tower's damage/range/rate, and **toolbar buttons stopped being `disabled` when Cycles are short** - a disabled button swallows its click, so the one tower you cannot afford would be the one tower whose stats you cannot read, which is backwards. They grey out via a class now; `isPlaceable` was already blocking the placement itself, so nothing became placeable that wasn't. `tsc` + `vite build` clean; still unplayed.
- 2026-08-23: **panel restyled as a console and moved into a dock with the toolbar** (user's call). Green-on-black terminal: dot leaders between label and value, scanline overlay, blinking cursor after the tower name, `[-]`/`[+]` collapse. Justified by [Theme](../Design/Theme.md) rather than picked for looks - green is already the powered/active trace color, so the readout glows like a live trace next to the cyan board it describes; no new colors entered the palette (green for live values, amber for the next tier, magenta for "cannot afford", all already in use). System monospace stays: a real terminal face would be a downloaded font, and in an offline-first PWA that would be the only asset in the project able to fail to load.
- 2026-08-23: **reversed the panel's anchoring decision from earlier today.** The spec put it top-right, opposite the toolbar, so the two would never fight for an edge. Watching the layout described back made it obviously wrong: they are one control surface, and opposite corners made the eye cross the whole board to get from "which tower" to "what it does". They now share a bottom-centered `.dock` with one width and one safe-area rule; the dock is `pointer-events: none` so the gap between panel and toolbar doesn't swallow taps meant for the board. The cost is honest and is why collapse exists: the dock is taller than the toolbar was and covers more of the bottom-center of the board. Two layout bugs caught while writing it - `width: max-content` on the dock let the stats grid demand all four columns on one line and stretched the console, fixed with a definite `min(94vw, 320px)`; and `display: flex` on the author side outranks the UA's `[hidden]` rule, so the actions row needed its own `[hidden]` declaration to actually hide in build mode.
- 2026-08-23: **this restyle is entirely visual and has been seen by no one** - the Chrome extension stayed disconnected all session. For a logic change `tsc` clean is worth something; for scanlines, glow, dot leaders and a collapsed header it is worth nothing. Treat every visual claim in the two entries above as intent, not observation.
- 2026-08-23: **enemies are now inspectable, and the PLACE line was cut back to the exception** (both user's call). The console takes a tower-or-enemy selection union instead of a lone `selectedTower`, so the two can never disagree; a selected enemy clears itself when it leaves play, and one `removed` check covers both dying and breaching the core. Enemy mode shows name, live HP in the header (the one number that moves while you watch, so it stays readable collapsed), speed, bounty, and whichever special rule applies - `IMMUNE AES TURRET` on the Zero-Day, `SPLITS 2x ENCRYPTOR` on Ransomware. Both of those were previously unknowable from inside the game: a player watching AES Turrets retarget away from the boss had no way to learn why, and Ransomware doubling on death just read as a bug. `enemy.ts` gained display names and an `immuneTowerKind` getter over the same table `isImmuneTo` already reads.
- 2026-08-23: two decisions inside that one worth keeping. **Tap order is tower, then build, then enemy, then clear** - building outranks inspecting because the Zero-Day sprite is 40px in a 32px trace and overhangs onto buildable tiles, so resolving enemies first would cost you a placement every time the boss walked past; enemies stay tappable over the trace, where nothing is placeable anyway. And `clientToGrid` was **split rather than duplicated**: `clientToWorld` returns the fractional position, `clientToGrid` floors it, because entities sit between tiles and tile resolution can't hit-test them. Hit radius is the sprite's larger half-side floored at a third of a tile, since a Packet Sniffer is 10x6 virtual pixels and is otherwise unhittable by a thumb.
- 2026-08-23: `PLACE OFF PATH` removed from three of four towers - it was true, and printing a rule that always holds buried the one that doesn't. Only Honeypot still prints `PLACE ON PATH`.
- 2026-08-23: **v1.2 pushed and played on a phone for the first time** - `ad857e8..4552624`, the whole milestone, nine commits that until now had only ever been type-checked. First real verdict on the dock: it covers a fair slice of the bottom-center board but is playable, so the console stays where the previous entry put it. The fix that came out of the playthrough is a dismissal gesture, not a layout change.
- 2026-08-23: **tapping the selected tower or enemy again dismisses it and folds the console to its header** (user's call, from playing it). The reasoning for making the re-tap the dismissal, rather than adding a close control: the readout you have just finished reading is exactly the one you want off the board, and the tap that opened it is already under your thumb - a second control would be one more 44px target competing for the same corner the header collapse already owns. All board selection now goes through one `select()` in `main.ts` that sets the selection and drives `panel.setCollapsed()`, and `Escape` routes through it too so the keyboard out and the touch out leave the console in the same state. A *new* selection always expands, because collapsed hides the stats and the `UP`/`OC`/`SELL` row both - tapping a tower to act on it and getting a folded header would be a dead end. Selling and placing still clear the selection without folding: those aren't dismissals, and the build preview is what you want on screen right after either one.
- 2026-08-23: **step 6 stays open.** The phone pass so far is one verdict - the dock's board coverage - and that is not the checklist the step was written for. Still unconfirmed by eye: whether the tier pips read against each tower (the AES Turret above all), whether a tap on a tower reliably selects rather than placing, whether the dock clears the notch in landscape, whether building still wins over the Zero-Day's overhanging sprite, and the upgrade-to-3 / sell / Overclock / restart loop end to end. Ticking the item on the strength of one answer is exactly the drift the docs move of 08-20 was meant to stop.
- 2026-08-23: **the dock lasted one playthrough - build menu moved to the left edge, console kept the bottom center** (user's call, from the phone). Third anchoring in a day, and the reversal is worth reading as a pair with the one above it: merging the panel and toolbar into one dock was right about the *relationship* (they are one control surface, opposite corners made the eye cross the board) and wrong about the *geometry*. Stacked, the two together ate the whole bottom band, which is where the trace runs into the core - the part of the board you least want covered. Split, each takes an edge the other isn't using and neither grows into the middle. What the dock existed to express survives without it: they still read as one surface because they share the terminal palette, border, glow, and 44px targets, which was always the thing doing that work - the shared container was just the first idea for how.
- 2026-08-23: `.dock` deleted rather than kept as an empty wrapper. It had a real job (`pointer-events: none` so the gap between the two didn't eat taps meant for the board) that stops existing the moment there's no gap, and a wrapper around one child would have been a name that lies about the layout. `.panel` and `.toolbar` now position themselves; the shared width, font and safe-area rules moved onto the two-selector rule they already shared. Toolbar is vertical and centered on the left edge, clearing the HUD text in the top-left. `createTowerPanel`'s second argument stopped being `dock` and became `host` for the same honesty reason.
- 2026-08-23: **the two layout changes above have not been seen running either** - the Chrome extension was still disconnected, and `vite build` clean says nothing about where a fixed element lands on a 360px-wide screen. Specific things to look at on the phone before believing any of it: whether the vertical toolbar clears the HUD's three lines on a short landscape viewport, whether the left column overlaps the spawn port, and whether the console at 320px still leaves usable board above it.
- 2026-08-23: **the build menu now hugs the board's left edge instead of the window's** (user's call). The gap it closes is the letterbox: the world blits at an integer scale into a centered rect, so on most screens there is a black band between window edge and board edge, and a menu pinned to the window sits out in that band - further from the thing it builds on than it needs to be, which is the whole complaint. New `boardRect()` in `canvas.ts` turns the blit's device-pixel offset and scale into a CSS-pixel rect, since DOM chrome and the renderer had no shared unit before this.
- 2026-08-23: the placement rule is **split between JS and CSS on purpose, rather than computed whole in either.** JS publishes one number - the board-hugging candidate - as `--toolbar-left`; the floors stay in CSS as `max(8px, env(safe-area-inset-left), var(--toolbar-left, 0px))`. Doing it all in JS would have meant reading the safe-area inset from script, which `env()` does not expose to an unregistered custom property; doing it all in CSS would have meant knowing the toolbar's own width in a `left` calculation, where percentages resolve against the viewport rather than the element. Splitting it lets each side answer the part it can actually see, and `max()` is the whole arbitration: tuck into the letterbox when the band fits the menu, sit on the board's edge when it doesn't.
- 2026-08-23: known and accepted - **in portrait there is no side band to tuck into**, so the menu still overlaps the board's left edge there. A 16:9 board on a tall screen letterboxes vertically, and moving the menu into *that* band would mean an orientation-dependent layout, which is a bigger change than the complaint asked for. Landscape is where the game is 16:9 and where the band exists. Unverified by eye like everything else today: the extension is still disconnected, and this change is entirely about where a box lands on screen.
- 2026-08-23: **v1.2 closed - step 6 verified on a phone and reported clean.** The playthrough was the user's, not this session's; recorded here as reported rather than as observed, since the Chrome extension has been disconnected all day and no session has seen the game running. This also discharges the multi-size/DPR and physical-device QA that v1.1 owed and v1.2 absorbed, so nothing is carried forward into v1.3.
- 2026-08-23: **v1.3 chosen - the run: wave curve + pacing.** Picked from arithmetic rather than taste: a run is 4 waves and pays out ~228 Cycles on top of the 100 you start with, while taking a single AES Turret to tier 3 costs 210 (45+65+100). The three-tier ladder v1.2 shipped is content the run is too short to reach - not a tuning problem but a milestone with nowhere to happen. It is also the debt v1.2 named on the way out ("upgrades change the economy's shape, so tuning the four demo waves before that settles would be wasted twice"); upgrades settled. Specced in [Run Structure](../Design/Run%20Structure.md), seven-step build order in [Roadmap](../Roadmap.md).
- 2026-08-23: the v1.3 ordering argument, since three of the steps could plausibly go first. **Pause + 2x lands first** because it is the tool every later step is tested with - a ten-wave curve tuned by sitting through ten waves in 1x is a curve tuned by patience. **The sim extraction lands second and is required to change nothing**: the sim currently lives in a closure inside `startGame()` reading a dozen locals, so nothing outside a browser can step it, and a refactor that arrives with a feature attached is a refactor nobody can verify. **The harness lands third**, before the curve, so the curve is tuned by number instead of by vibe. **Preview before early-call bonus**: paying a player for a decision whose terms are hidden is a slot machine, not a decision.
- 2026-08-23: the balance harness turned out cheaper than the Idea Bank entry assumed - **the simulation is already deterministic.** The only `Math.random()` in `src/` is in `effects.ts`, scattering glitch particles, and those never feed back into the sim. So there is no seeded RNG to build: fixed timestep in, same result out. The real cost of the harness is the extraction, which is worth doing on its own (`main.ts` is 435 lines and the biggest file in the project). It also brings the first tests the project has ever had.
- 2026-08-23: also decided what v1.3 will *not* do. **Audio** - the biggest learning module left and the loudest gap in a portfolio piece, which is why it gets its own milestone instead of a corner of this one. **New enemy behaviors** - adding units while tuning a curve is balancing against a moving target, the same trap v1.2 dodged by refusing to touch waves. **Endless mode and high scores** - both measure a run against a standard, and the standard is what this milestone builds. **A second map** - the waypoint format is ready, but a new map inheriting a curve should inherit a finished one.
- 2026-08-23: v1.3 step 1 done - **pause and 2x.** `GameLoop.setTimeScale()` scales the **accumulator**, not the tick: every tick is still `TICK_MS` and 2x just runs twice as many of them per frame. Scaling the tick instead would have been one character shorter and would have made the same run produce different numbers at different speeds - which the balance harness two steps from now cannot live with. Pause is `setTimeScale(0)`: no time accumulates, so no ticks run, while `render` keeps being called every frame, which is the whole point (the board, the console and the current selection stay readable with the clock stopped). `lastTime` still advances while paused, so resuming doesn't spend the pause catching up on time the player spent thinking.
- 2026-08-23: the controls went **in the console's header, not the build menu and not a new box.** The build menu answers "what do I place"; speed and pause are statements about the run, which is what the console already reads out. Header rather than body because the body is what collapsing hides, and a paused game whose only way back is to expand a console first is a bug wearing a preference as a disguise. Cost, stated plainly: `.panel-head` went from a 34px title strip to a 44px control bar, so the collapsed console covers ten more pixels of board. That is the trade for the two buttons being real tap targets, and it is the same bottom band the dock was killed over - worth watching on the phone.
- 2026-08-23: the header stopped being a `<button>` and became a `<div>` holding one - a button cannot contain a button, and the collapse control had to keep its 44px bar while pause and speed sat beside it. Collapse now takes whatever width the two run buttons leave.
- 2026-08-23: **pause shows the action, speed shows the state** - `||` when running, `>` when paused, against `1x`/`2x`. Not an inconsistency worth fixing: everyone reads a play glyph as "start this", while "2x" is a reading whose alternative has no glyph. Both light amber when they are the reason the run is not behaving normally, reusing the Overclock/next-tier colour rather than adding one.
- 2026-08-23: three decisions inside step 1 worth keeping. **Building, upgrading and selling all still work while paused** - planning with the clock stopped is most of what a pause is for in this genre, and the sim being stopped makes it harmless. **Restart resumes but keeps the speed**: pause is a state and restarting into a frozen board would read as the tap-to-restart having failed, while 2x is a preference. And `Space` **defers to a focused button** rather than blurring it after a click - blurring would toggle pause on a keypress the player aimed at a button, and would also throw away a keyboard user's place on the page.
- 2026-08-23: known deviation from what "paused" implies, recorded rather than fixed: sprite idle frames and the IDS Scanner's sweep are driven by `performance.now()` in the render pass, so they keep animating while the sim is stopped. Enemies do not move (their `distance` is frozen) but they do keep breathing. Fixing it means the render pass reading simulated time instead of wall time, and simulated time is exactly what **step 2 creates** - so it belongs there, not in a special case here. It may also turn out to be worth keeping: a board that is frozen but still alive is a board that has visibly not crashed.
- 2026-08-23: `tsc` and `vite build` clean, and **not seen running** - the Chrome extension was disconnected again, same as every session yesterday. What that leaves unverified is the visual half: whether the 44px header still reads as a title bar rather than a toolbar, whether `||`/`1x` are legible at 12px on a phone, and whether the ten extra pixels of collapsed console matter on the bottom band.
- 2026-08-23: v1.3 step 2 done - **the simulation moved out of `main.ts`.** New `game.ts` owns an explicit `GameState` plus `stepGame(state, dtMs, hooks)`, and the mutations input used to perform inline - `placeTower`, `sellTower`, `upgradeTower`, `towerAt`, `isPlaceable`, `findTarget` - moved with it, because the harness needs to build a layout without a pointer event. `main.ts` went 435 lines to 341 and now does input, selection, DOM and drawing only. Restart became `state = createGameState(level1)` instead of resetting nine variables by hand, which is one fewer way to forget one.
- 2026-08-23: the one thing that could not simply move: the kill burst scatters the dead enemy's **sprite pixels**, and those live in the baked atlas - a canvas. So `game.ts` takes a `GameHooks.enemyPixels` and imports `sprites.ts` for its type only. Headless there is no hook, no burst, and therefore no `Math.random()` anywhere the simulation can reach: the determinism the balance harness needs isn't a property to be engineered, it's what is left when the cosmetics are unplugged.
- 2026-08-23: verified in a browser for once - **the extension was connected.** Placed (100 to 80 Cycles), selected, upgraded (80 to 50, `TIER 2/3`, `2 > 3` damage, sell rising to +30), ran a whole wave set to `SYSTEM SECURED`, and a second run to `GAME OVER` on a core breach. Also confirmed **pause on the board**: HUD reads `WAVE 1/4 - PAUSED`, the header button flips to `>`, and four towers were placed while the clock was stopped, which is the behaviour step 1 chose on purpose. Headless, the same sim ran to completion in Node twice with identical numbers.
- 2026-08-23: an artefact of testing this way, worth knowing before the next session wastes twenty minutes on it: **Chrome pauses `requestAnimationFrame` when its window is fully covered** by another window, so between two tool calls the game genuinely freezes and reads exactly like a broken loop. It is not one. Driving a real frame budget means keeping a rAF loop alive from the page itself while the screenshot is taken.
- 2026-08-23: **the kill burst got a test instead of more screenshots** (user's call, and the right one: the effect lives for 380ms and three attempts to catch it in a screenshot all missed). `tests/game.test.ts`, run with `npm test` on Node's own runner - no framework, no new dependency. Three gates: a kill produces particles whose colours all come from the pixels the hook handed over; with no hook there is no burst *and the run is otherwise identical*, tick for tick, so a cosmetic can never move a number the harness reads; and the same run twice produces the same result. Mutation-checked rather than assumed - forcing the hook's result to `undefined` fails the first test and only the first.
- 2026-08-23: tests live in `tests/`, outside `tsconfig`'s `include: ["src"]`, so they are **not type-checked** - the alternative was adding `@types/node` to a project that has three dev dependencies on purpose. Recorded as a real gap rather than a choice that needs no revisiting: step 3 builds the harness in the same place and will have to answer the same question with more code at stake.
- 2026-08-24: v1.3 step 3 done - **the balance harness.** `npm run balance` runs the whole curve headless against a declared tower layout and prints, wave by wave, the size of the wave, the peak live enemy count, leaks, Cycles earned, Cycles spent, the balance left and what got bought. No new dependency: the run is `stepGame` in a `while` loop at `TICK_MS`, which is all it can be now that the sim is out of `main.ts`.
- 2026-08-24: the harness is **split in two, and that split is the point**: `src/balance.ts` holds the run and the layouts, `scripts/balance.ts` holds argv and `console.log` and nothing else. That answers the gap the last session recorded - tests and scripts sit outside `tsconfig`'s `include: ["src"]`, so they are not type-checked - without adding `@types/node` to a project with three dev dependencies. Everything with a number in it is checked by `tsc` alongside the simulation it measures; what is left unchecked is column padding. Vite never bundles `balance.ts` because nothing reachable from `main.ts` imports it - checked, not assumed: the harness vocabulary (`loadout`) appears nowhere in `dist/`.
- 2026-08-24: a layout is a build **order, not a set** - entries are bought in sequence as the Cycles arrive, and an entry the player cannot afford blocks the ones behind it. That was a choice with an alternative (buy whatever is affordable, skipping ahead), and priority order won because it turns the harness from "does this set of towers win" into "is this tier reachable, and by which wave" - which is the actual question v1.3 exists to answer. `fromWave` holds an entry back until a given wave, for layouts that mean to save.
- 2026-08-24: `TICK_MS` moved from `game-loop.ts` to `game.ts` on the way past. It is a property of the simulation, not of the browser loop that happens to drive it, and it was about to be copied into a third file.
- 2026-08-24: first readings, and the milestone's premise **confirmed in numbers rather than by feel.** `bare` (nothing built) loses in 17 seconds, so wave 1 is a real question. `starter` - 65 Cycles of towers, never touched again - clears all four waves and finishes holding **198 Cycles, three times what it spent**. `focused` takes an AES Turret to tier 3 by wave 3 and still ends with Cycles banked. The run does not just fail to reach the tier ladder; it pays for the whole ladder and then runs out of waves to spend it on.
- 2026-08-24: reading worth carrying into step 4: `spread` (one of each kind at tier 1) **loses**, while `starter` (two Firewall Nodes and a Honeypot, 65 Cycles) wins comfortably. Under this curve, breadth is a trap and it is not clear the trap is on purpose - four tier-1 towers spread along the trace kill less than two stacked at the entrance. Whether that survives the new curve is a thing to check, not a thing to preserve.
- 2026-08-24: also surfaced by the harness and not by any amount of playing: **the boss leaking still counts as a win.** In both winning layouts wave 4 shows `LEAK 1` and the run ends `SYSTEM SECURED` - the Zero-Day walks into the core, costs one HP like a worm, and the wave is cleared because the spawner is done and no enemies are left. Recorded here for the wave-curve step to decide on, rather than patched in a harness commit.
- 2026-08-24: four tests for the harness in `tests/balance.test.ts`, on the same principle as the kill-burst tests: they gate the **instrument, not the reading**. Every loadout's books balance (banked = start + earned - spent, and the wave rows account for every Cycle and every leak), an undefended board loses, the build order actually blocks, and the same loadout reports the same run twice. Mutation-checked - dropping the spend accounting and dropping the per-wave leak attribution each fail exactly the accounting test.
- 2026-08-24: the blocking test needed rewriting once, which is the more interesting half. The first version asserted "the AES reaches tier 3, and the Firewall behind it is not bought first" - and a lone AES Turret never reaches tier 3, so it failed. The rewrite asserts what actually happens and is a stronger claim anyway: the Firewall is never bought at all, **while the run ends holding 69 Cycles against its cost of 20**. Affordable for waves, correctly refused.
- 2026-08-24: v1.3 step 4 done - **the ten-wave curve.** Shape rather than slope: Worms open, Trojans alone at 3, the first real mix at 4, Packet Sniffers alone at 5, a deliberate breath at 6, Ransomware alone at 7, a sniffer flood with Trojans inside it at 8, the crunch at 9, Zero-Day at 10. Every kind gets a wave to itself before it appears in a mix; the Encryptor is the exception because it has no group anywhere in the curve - it is only ever born from a Ransomware splitting, so the solo Ransomware wave is also its introduction.
- 2026-08-24: the spawner was rewritten, which was **not in the plan**. Groups used to run one after another - `groups: [worms, trojans]` meant all the worms and then all the Trojans, which is a queue, not a mix, and "introduced alone before it appears mixed" is a promise about mixing. Now every group has a `startMs` and runs on the wave's own clock, so wave 4 is Trojans walking through a worm stream instead of after one. `stepSpawner` returns the kinds due this tick (usually none, sometimes several) instead of one-or-null, and due times are computed from the group's start rather than accumulated per spawn, so a slow frame catches up instead of drifting.
- 2026-08-24: **`hpScale` per wave, also not in the plan, and the harness is the only reason it exists.** With the curve tuned on counts alone, `focused` - two Firewall Nodes upgraded to tier 3 at the entrance - won 5/5 in 1:51 with peaks of *two* enemies from wave 4 on. Counts could not fix it: three times the enemies is three times the bounty, and the economy is the other half of what this step is sizing. HP is the one lever that makes a late wave harder without making it richer. Waves 7-10 carry 1.5x / 2x / 2.75x / 2.5x; everything before stays 1x.
- 2026-08-24: the ramp's first draft started at wave 4 and was **far too brutal, for a reason worth writing down**: rounding. A 1.2x multiplier on a three-HP Worm is four HP, a 33% jump, and a tier-1 Firewall needs a fourth shot for it - wave 4 went from a mix to a wall that took all five core HP off a frozen board. Small multipliers are not small on small numbers. The ramp now starts at 7, where boards have tiers.
- 2026-08-24: **the boss finding from step 3 got its answer: a Zero-Day breach now costs 3 core HP**, not 1. Three rather than five on purpose - a run that arrives clean survives it at 2 HP, a run that has been leaking all along does not, so every earlier leak stays on the books until the last wave. New `coreDamage` on the enemy, and a `BREACH` row in the console's enemy readout, shown only when it is not the ordinary 1: v1.2 added enemy inspection precisely so rules like this stop being invisible.
- 2026-08-24: bounties came down (Worm 5 to 4, Trojan 12 to 10, Ransomware 6 to 5, Encryptor 3 to 2) and the boss's own HP went back to 40, with wave 10's 2.5x making it the 100 HP it needs to be. A flawless run now collects **409 Cycles** on top of the 100 it starts with. Topping out all four towers costs 570, the two dearest ladders cost 355 - so the purse lands between them, which is the entire decision the tier ladder exists to pose. `tests/wave.test.ts` computes both numbers from the data and fails if the gap ever closes from either side.
- 2026-08-24: the harness earned its keep and also **had a trap in it**: a build order entry on a tile an earlier entry already built could never place, so it blocked the rest of the order forever and reported itself as "never afforded" - which is a lie, and exactly the kind of lie that would have sent this tuning pass the wrong way. An entry on an occupied tile of the same kind now adopts that tower and upgrades it, which is what "take the Firewall at 2,3 to tier 3" means anyway.
- 2026-08-24: six loadouts now, and the spread between them is the reading. `bare` dies in wave 1. `starter` (the opening 65 Cycles, frozen) dies at wave 9. `spread` (breadth, everything tier 1) dies to the boss holding 185 unspent, because breadth runs out of things to buy. `turret` (bank for a tier-3 AES and stay tier 1 at the front) dies at wave 9 - the gamble loses. `focused` (pure depth on the opening three) wins at 2/5. `veteran` (upgrade what is already shooting, then widen) wins 5/5 with 164 banked. Only the last two clear, which is about the right width for a curve that is meant to be answerable and not free.
- 2026-08-24: four new tests in `tests/wave.test.ts` gate what the curve's comments claim: a wave spawns exactly what it declares, groups genuinely overlap (the offset group starts before the opener finishes), every kind's first wave is its own, and the ramp moves HP without touching speed or bounty. The finale is written into the introduction rule as an explicit exception rather than a silent one - the rule exists so a kind is understood before it arrives under load, and the last wave has nothing left to teach for; it may still introduce only one kind, and its escort must be kinds the player already knows.
- 2026-08-24: browser check was **half-blocked, in the way this project has hit before**. Placement, the console, the 2x toggle and the new HUD reading `WAVE 1/10` all confirmed, and a Worm kill paid `35 -> 39` Cycles, which is the new bounty on screen. The full playthrough was not: Chrome freezes `requestAnimationFrame` while its window is fully covered, so eighteen seconds of waiting bought about one spawn, and a `Runtime.evaluate` measuring the frame rate timed out against a renderer that genuinely was not running. Screenshots force a frame, which is why anything moved at all. The ten-wave playthrough belongs to step 7 anyway.
- 2026-08-24: v1.3 step 5 done - **the console's fourth mode.** With nothing selected and a countdown running it reads the wave that is coming: `WAVE 4/10` in the title, `IN 6s` in the meta, one dot-leader row per kind (`WORM ... x5`), and a `HARDENED ... 2.8x` row on the waves that carry an HP multiplier - same rule as the enemy panel's `BREACH` line, where only the exception earns a row. `nextWavePreview(spawner)` returns null whenever there is nothing to preview, so the panel falls back rather than reading the spawner's state itself.
- 2026-08-24: the mode order is tower, enemy, wave, build - and **the wave/build boundary needed a rule**, because the lull is exactly when a player reads tower stats and exactly when the preview wants the surface. One flag: picking a tower kind (toolbar or `1`-`4`) hands the console to the build stats, and each new countdown claims it back once. Dismissing a reading clears it too, so tapping away during a lull lands on the wave rather than back on stats you already walked away from. Verified both directions in the browser.
- 2026-08-24: **the between-wave lull went from 4s to 8s, and the opening from 3s to 5s.** Not cosmetic and not really optional: four seconds is not enough to read a composition and act on it, so the preview would have been a thing that flashes past. It is also the countdown step 6 is about to pay players to skip, and a bonus for skipping four seconds is not a decision worth posing. Checked against the harness: every loadout's core HP, leaks, earnings and banked Cycles are identical to before, only the run clocks moved (veteran 2:29 to 2:47).
- 2026-08-24: known and kept: dismissing during a lull **collapses the console**, so the composition folds away and only the header line survives. That is the v1.2 dismiss rule doing exactly what it was written to do - the player asked for the board back - and the header still reads `WAVE 4/10  IN 6s`, which is the headline anyway. Auto-expanding would override a preference to show something the player did not ask for.
- 2026-08-24: one more test in `tests/wave.test.ts`, walking the spawner through the whole curve: the preview is always one wave ahead of the one that just finished, is null while a wave is actually spawning, counts to the same total the wave spawns, merges two groups of the same kind into one row instead of listing it twice, and carries the wave's `hpScale`. Mutation-checked by pointing the preview at the current wave instead of the next - that fails, and only that.
- 2026-08-24: the browser check hit the covered-window freeze again but got what it needed anyway, because **the opening countdown is itself a preview**: a fresh load renders the mode with no simulated time required. Read `> WAVE 1/10  IN 4s` over `WORM ... x5`, tapped `AES` and got the build stats, pressed Escape and got the wave back. The transition that still has not been seen with real eyes is the one at the end of a wave, which needs a wave to end.
- 2026-08-24: v1.3 step 6 done - **the early call.** The wave preview's action row grows a single `CALL +8` button (the other three actions hide; a wave that has not arrived cannot be sold or upgraded), `callWaveEarly(state)` pays the bonus and drops the countdown to zero so the next tick opens the wave through the same branch a countdown running out would have taken. One Cycle per whole second skipped, worth about 77 over the ten waves against a curve that pays 409.
- 2026-08-24: the bonus is rounded **up**, deliberately, because the button's `+8` and the meta's `IN 8s` are one number read twice and two readings of the same countdown disagreeing by one is the kind of small lie that costs trust in every other number on the panel. `earlyCallBonus(ms)` is the single source both read, and a test pins the rounding.
- 2026-08-24: **the harness says the early call is currently free money, and this step did not fix it.** `rush` and `veteran` share the same `Build[]` array and differ in exactly one field, so the comparison is controlled: same towers, same purchases, same 5/5 core and zero leaks - and `rush` collects 77 more Cycles and finishes 1:15 sooner. There is no mechanical cost, because Cycles only arrive from kills and the countdown only starts once the board is clear, so the seconds being skipped were not producing anything.
- 2026-08-24: the cost the design intended is real but **invisible to the harness**: the lull is reaction time, and a scripted build order that buys the instant it can afford has infinite reaction time. The harness's report is the ceiling for a perfect player, not the median - worth remembering the next time a loadout's number is read as a verdict. Two ways to make the cost mechanical were considered and both rejected as out of this step: starting the countdown when a wave finishes *spawning* rather than when the board clears (retunes the whole curve that step 4 just settled), and allowing the call during `waiting-clear` so an early wave overlaps the stragglers (a genuinely interesting opt-in risk, and a design change the milestone did not ask for). Filed rather than smuggled in.
- 2026-08-24: three tests in `tests/game.test.ts`: the call pays exactly what the preview advertised and the wave opens on the next tick, a wave already on the board is not for sale, and the rounding matches what the console prints. The harness gained a `callWavesEarly` flag on a loadout, which is what made the free-money finding a measurement instead of a suspicion.
- 2026-08-24: seen working in a browser, again off the opening countdown: `CALL +4` under `WORM x5` with `IN 4s` beside it, tapped, Cycles went 100 to 104 and wave 1 opened. The console fell back to the build stats on its own, because with the countdown gone there is no wave to preview.
- 2026-08-24: **the curve was played end to end and ran clean** - reported by the player, not by the harness, which is the confirmation step 4 was actually waiting for. Two changes asked for off the back of it, both landed the same day.
- 2026-08-24: the HUD went from three labelled lines in the top-left (`CORE 5/5`, `CYCLES 100`, `WAVE 1/10`) to **three glyphs in a bar at the top center**: a pixel heart, a coin with a C in it, and `Lv 1`. The denominators went with the labels on purpose - the core's maximum and the curve's length are facts about the game, not about this second of it. Colours are the ones already in the palette carrying the meanings they already have: the core is the magenta of the core sprite, Cycles are the amber the console uses for values, the wave is terminal green.
- 2026-08-24: it moved from canvas to **DOM**, which is the change that mattered rather than the glyphs. `drawHud` painted text into the display canvas; the bar is now `hud.ts` next to the toolbar and the console, sharing their box and their reasoning ("real elements get native touch handling and safe areas for free"). What is left in `render.ts` is `drawEndScreen`, which is the one thing that genuinely belongs on the board. The heart is a 7x6 pixel grid expanded into SVG rects with `shape-rendering: crispEdges`, so it is the same art as everything else rather than an icon font wandering in.
- 2026-08-24: and the point of the move: **the wave indicator is a button.** Tapping `Lv 4` puts the wave in the console. The automatic preview only ever exists during a countdown, so until now there was no way to ask "what am I in the middle of" - `currentWaveReading` answers with the incoming wave when one is counting down and with the wave on the board otherwise, headed `ON BOARD` instead of `IN 6s` and with the `CALL` row gone, because a wave already walking the trace is a reading with nothing to decide.
- 2026-08-24: that is a third flag deciding who owns the console (`selection`, then `waveFocus`, then `buildFocus`, then the automatic preview). Three booleans for one surface is the point at which this stops being free, and the next thing that wants the console should probably arrive as one explicit mode rather than a fourth flag. Noted now, while it is still three.
- 2026-08-24: the bar parks with the **same trick as the build menu, on the other axis** - it publishes a candidate `--hud-top` just above the board and CSS `max()` picks between that and the safe-area floor, so it tucks into the letterbox band when there is one and sits on the board's top edge when there isn't. Font is fixed at 16px rather than scaled with the board: the DOM chrome holds its size and the world grows around it, which is what the toolbar already does.
- 2026-08-25: **v1.4 chosen - portrait.** No code this session; the milestone was talked through, drawn, and written down, which is the order the project asked for. The request: on a phone held upright, glue the console to the board instead of to the window, move the menus, and let the phone's own chrome be hidden. It is the debt this log accepted in as many words on 2026-08-23 - "in portrait there is no side band to tuck into [...] moving the menu into *that* band would mean an orientation-dependent layout, which is a bigger change than the complaint asked for". Specced in [Portrait Layout](../Design/Portrait%20Layout.md), seven-step build order in [Roadmap](../Roadmap.md).
- 2026-08-25: **the design moved three times during the conversation and the intermediate versions are kept on purpose**, in the spec and in the roadmap. Each one died to a number, and the numbers are the point: this is a milestone that was designed by measuring the screen rather than by picturing it. What follows is that sequence, because the reasons matter more than the destination.
- 2026-08-25: **draft 1: rotate the tower buttons' labels.** That was the request's own headline - a 90-degree label so the left column could shrink into portrait's side band. Costed and dropped: a rotated column lands around 30-40px against 24px of band, so the overlap shrinks without going away, and the button's short axis falls under the 44px target the rest of the UI keeps. Paying an accessibility rule for a partial fix is a bad trade. Reversed by the player once the numbers were on the table, not decided over their head.
- 2026-08-25: **draft 2: keep the column, accept the overlap.** Written into the spec, and then killed by drawing it to scale in ASCII, which is the cheapest thing anyone did today. The toolbar is 206px tall against a 192px board and 40px wide against a 21px tile, so it covers the board's full height and 1.9 tiles of its width - and the path starts at `{x: 0, y: 2}`, so it hides the tile enemies walk out of. The drawing also caught a 1px collision between the toolbar's foot and the console's new top edge that the arithmetic had missed. **Draw the layout before speccing the layout.**
- 2026-08-25: **draft 3: the menu comes off the board.** Caused by a misreading worth recording - "the towers stay in the same place" was read as the tower *buttons* and meant the towers *on the board*. The player was saying the same thing twice ("the grid stays where it is"), which is that the board does not move and the chrome does. With the menu free to leave the left edge, both problems above evaporated: it becomes a horizontal row below the console, in the band that was empty anyway.
- 2026-08-25: **and then the board rotated, which changed the milestone's shape.** The arithmetic that decided it: the world is 512x288 blitted at an integer scale, so on a 390x844 phone at dpr 3 a landscape board upright solves to scale 2 (341x192, **21px tiles**, a quarter of the area and half the 44px tap target) while a board rotated 90 degrees solves to scale 4 (384x683, **42px tiles**) - the same size as the landscape board, with nothing lost. The ceiling that made portrait the lesser orientation was the window's *width*, and rotating the board is what removes it.
- 2026-08-25: the trade, taken with eyes open: **the rotated board eats exactly the band the chrome was going to move into.** 326px of dead space above and below becomes 81px. So the third draft's roomy stack - board, console, menu, all in a column with slack - is gone, and the chrome fits only because it collapses. A middle option was costed and refused: dropping one scale step to 288x512 gives 166px bands where nothing ever overlaps, at 32px tiles. **42px tiles with dismissible chrome beat 32px tiles with permanent chrome**, and the player made that call directly.
- 2026-08-25: which is why the player's last suggestion - a button that opens and closes the build menu - stopped being a convenience and became **structural**. 81px holds one 44px row and nothing else: the status bar and the menu's toggle share the top band, the console's collapsed header takes the bottom one, and opening either covers 1.5 to 2 tiles of the board's near edge. That is the same trade landscape already makes and the player already accepted ("covers a fair slice of the bottom-center board but is playable") - the difference is that now there is a gesture to take it off the board.
- 2026-08-25: **the bands stay reserved for the collapsed state**, so a drawer opening never re-scales the world. Written into the spec as its own paragraph because the alternative is cheap to implement by accident and is the worst thing this layout could do: a board that resizes mid-wave because a menu opened. The menu also does not self-close after a tower is placed - building three towers should not cost three reopenings, and the console already behaves this way. Filed for verify, since how a gesture feels is not a question a spec can answer.
- 2026-08-25: **it rotates counterclockwise: spawn at the bottom, core at the top, enemies climbing.** Clockwise is the intuitive reading and puts the core at the bottom of the screen, which is exactly where the console opens - the console would cover the thing being defended, every time a tower is read. The cost is that the run's direction inverts against landscape, left-to-right becoming bottom-to-top, which is a reading decision rather than a pixel one. Verify owns it and can overturn it.
- 2026-08-25: the console's rule survived every draft unchanged, and is the one piece of this that is orientation-agnostic. The status bar and the build menu publish a candidate off `boardRect()` and let CSS `max()` pick against a floor; the console cannot copy that one-liner because it is anchored by its **bottom** and grows upward, so gluing its top under the board needs its own height - which changes with every collapse and every one of its four modes. The rule inverts to `top = min(boardBottom + gap, windowHeight - panelHeight - floor)` with a `ResizeObserver` republishing the measured height, and **landscape must come out unchanged to the pixel**. That is the step's checkpoint, rather than "it looks right in portrait".
- 2026-08-25: every branch in this milestone is a **measured predicate, not a media query** - rotate when the window's aspect and the board's disagree, stack when the band that is left can hold what wants to stand in it. Asking about the room that exists rather than about which way the device is held gets the right answer for a short landscape browser window on a tall phone, a narrow desktop window, or a tablet, all of which an orientation query gets wrong on its own.
- 2026-08-25: "automatic, with a way to lock it" was the answer to what switches the layout, and building it revealed the question had no referent: **nothing switches**. Every box positions itself by arithmetic off `boardRect()`, so rotating the phone re-runs the same formula and the automatic half was already free. What survives is a genuinely different feature, a **screen orientation lock** (`screen.orientation.lock()`), for a phone rotating under a hand resting on its side. Recorded as a reframing rather than as the thing that was asked for.
- 2026-08-25: sequencing, since several steps could plausibly go first. **The manifest unlock lands first** and is a prerequisite rather than a feature - it says `orientation: 'landscape'`, so the installed PWA cannot be held upright at all and nothing after it is testable where it matters. **The rotation lands second** because every later step is positioned against the board it produces. **Fullscreen before the lock** because `screen.orientation.lock()` rejects on Android outside fullscreen or an installed app and does not exist on iOS, so both buttons are feature-detected and desktop keeps the header it has. Fullscreen also earns more here than it would have: an Android nav bar is ~48px of an 81px band.
- 2026-08-25: also decided what v1.4 will *not* do. **A portrait-shaped map** would avoid the sideways reading entirely and the waypoint format would take a 9x16 level today, but a second map inheriting a curve should inherit a finished one - the same reason v1.3 gave for refusing map 2, and the rotation gets the space for free anyway. **Re-authoring sprites for the rotated board** is a second set of art, not a layout milestone; the blit rotates the trace, the core and the towers, while enemies come out right by accident because they already orient to travel direction. **v1.3's verify is not folded into v1.4's** either - the v1.1-into-v1.2 precedent applied because the interaction under test was about to be deleted, and here the ten-wave curve is a gameplay check in landscape that nothing in this milestone disturbs.
- 2026-08-25: **last change, and it fixed something two milestones had been flagging.** The player moved the build menu to the bottom and asked for the glyphs centered up top, which is one 44px row per band - and the bottom band, with a notched phone's 34px safe-area inset eating most of its 81px, has room for exactly one row and no second. That forced fullscreen and the orientation lock **out of the console header**, where this spec had been about to put them, and up into the top band beside the glyphs. The header goes back to three things; the log had already flagged that row as crowded while there were still three in it. The split also lands the right way round on its own merits: readings at the top, controls under the thumb, and the two settings touched once a session as far from the thumb as the screen allows.
- 2026-08-25: the open build drawer is **44px tall, not the column's 56**, so the label and cost sit on one line (`FW 60`) instead of stacked. The stacked form exists because a vertical column has width to spare and no height; a horizontal drawer has the opposite problem, and a drawer has to match the strip it grows out of.
- 2026-08-25: **v1.3 closed.** The player ran the ten-wave curve on desktop and phone, including a 2x run and a run calling every wave early, and reported it clean. So v1.4's first two steps went in on top of a finished milestone rather than beside an open one, which is what the spec asked for when it refused to fold the verify forward.
- 2026-08-25: v1.4 step 1 done - `orientation: 'landscape'` became `'any'` in the manifest. One line, and the reason it goes first: installed, the game could not be held upright at all, so every step under it would have been untestable in the only place it matters.
- 2026-08-25: v1.4 step 2 done - **the board turns a quarter turn counterclockwise when the window is upright.** `Viewport` gained one flag, `present()` one branch, `clientToWorld()` its inverse, and `boardRect()` swaps its axes through a new `displayedSize()`. Nothing outside `canvas.ts` learned about it: the sim, the renderer and the hit-testing all still speak world coordinates. On the spec's reference phone the board goes from scale 2 (341x192, 21px tiles) to scale 4 (384x683, 42px tiles) - the same size it has lying down.
- 2026-08-25: **deviation from the spec, and it made the checkpoint provable.** The spec's predicate was aspect disagreement - rotate when the window is upright and the board is not. Implemented instead as *rotate when turning buys a bigger blit scale*, computing both candidate fits and breaking the tie toward leaving the board alone. Same answer on every phone, but a tall desktop window with room for the board as it stands - 1400x1500, where both candidates floor to scale 2 - keeps it, instead of inverting the run's direction for nothing. It also turns "landscape takes the branch zero times" from an argument into arithmetic: a window wider than tall can never win a bigger scale by turning a 16x9 board.
- 2026-08-25: the rotation is an explicit matrix, `setTransform(0, -s, s, 0, ...)`, not `rotate(-Math.PI / 2)`. `Math.cos(-Math.PI / 2)` is 6e-17 rather than 0, and a hair of skew is enough to make the sampler resample every pixel of a pixel-art blit - the exact thing the virtual-resolution renderer exists to prevent.
- 2026-08-25: counterclockwise as specced, so the world's left edge lands at the bottom of the screen: spawn under the thumb, core in the top band. Clockwise reads more naturally and parks the core exactly where the console opens over it.
- 2026-08-25: `tests/viewport.test.ts` - the first tests the project has that are about the *renderer*, which is possible because fitting and hit-testing are arithmetic over four numbers and the only DOM they touch is a width, a height and a dpr (stubbed). Five of them: landscape reproduces the pre-v1.4 formula exactly for six window/dpr combinations, the reference phone lands on the spec's own table (scale 4, 384x683, 42px tiles), a roomy tall window is left alone, taps round-trip through the blit in both orientations, and the spawn sits below the core on a turned board. The spec's arithmetic table is now a test rather than a paragraph.
- 2026-08-25: not seen running - the Chrome extension refused to open a tab (`Tab creation is restricted in standalone sidebar mode`). Second milestone in a row where the browser check fell to the player. The chrome overlapping the board in portrait is expected here and is steps 3 and 4's job; nothing was done about it in this commit.
- 2026-08-25: v1.4 step 3 done - **the console parks against the board instead of the window.** It was the last box still pinned to the window; in landscape the two coincide, which is why it survived three milestones. The rule had to invert rather than copy the status bar's: those publish a candidate and let CSS `max()` pick it against a floor, because they are anchored by the edge they hug. The console is anchored by its bottom and grows upward, so its *top* depends on how tall it currently is - and that changes with every collapse and every one of its four modes. So JS publishes `--panel-under-board` and a measured `--panel-h`, CSS takes `min(underBoard, 100% - panelH - floor)`, and `env(safe-area-inset-bottom)` stays inside the floor where it already lived.
- 2026-08-25: one `ResizeObserver` on the console covers collapse, mode switches and reflow. The alternative was calling the publish from every place that might change the height - the collapse toggle, the four mode switches, the stats grid - which is a list that grows every milestone and is wrong the first time someone forgets. Height 0 is not published: 0 means hidden, and publishing it would park the console at the window floor for the frame after it comes back.
- 2026-08-25: the checkpoint held with a caveat worth writing down. A 16x9 window lands on the floor and the console sits exactly where it always did. But a **letterboxed** window - a 4:3 desktop, or portrait - is one where the board stops well short of the bottom, the board candidate wins, and the console moves up to hug it. That is not the checkpoint failing, it is the feature: the checkpoint means landscape-as-played does not move, not that the formula never fires outside portrait.
- 2026-08-25: v1.4 step 4 done - **the build menu lies down when the board turns.** One `turned` class on `body`, toggled by `applyLayout()` off the renderer's own `viewport.rotated`, and CSS holds both layouts. The predicate is honest rather than a media query: a turned board leaves a 3px side band, and no column stands in 3px. The buttons go `flex-direction: row` so `FW 60` sits on one line, and a 44px `BLD [-]` toggle takes the strip's left end using the console's own two glyphs, so the two drawers down there say "open me" and "close me" in one vocabulary.
- 2026-08-25: **the collision the spec did not settle, and how it was settled.** The bottom strip holds the toggle and the console's collapsed header, and both drawers were specced to open upward into the board - into the same space. Mutual exclusion was the obvious cheap answer and it is wrong: picking a tower kind is *what puts that kind's stats in the console* (v1.2's build preview), so a drawer that collapsed the console would hide the reason for opening the drawer. So the drawer **stacks on the console's ceiling** and rides it. The cost is that expanding the console pushes the build row up; in practice the console is expanded whenever anything is selected, so the movement lands on the player's own taps rather than arriving unasked.
- 2026-08-25: that ceiling is now a named CSS value, `--panel-top`, holding the same `min()` the console was doing inline - the console's top and the drawer's bottom are one number and can't drift. It carries a `100%` inside a custom property, which resolves against the viewport at *use* time and is correct for both because every box using it is `position: fixed`.
- 2026-08-25: with the console hidden - the frame before the first reading, and the end screen - `--panel-h` falls back to 0 and the drawer drops to the strip's floor. Kept rather than papered over: with no console down there, the strip is exactly where the drawer belongs.
- 2026-08-25: the console shifts off centre in portrait to leave the strip's left end to the toggle: `left` past the toggle plus a gap, width `min(320px, ...)`, which on the reference phone still comes out at the full 320.
- 2026-08-25: still not seen running - the extension refused again with the same `standalone sidebar mode` error. Three steps of layout arithmetic now rest on the player's eyes, which is the wrong ratio; the milestone's verify step is where that gets paid, and it is next.
- 2026-08-25: **the player caught the drawer covering board it did not need to.** The console hugs the board's underside, which on a letterboxed window leaves unused band *below* the console - and the drawer, stacked on the console's ceiling, was pushed up onto the board while that band sat empty. Backwards: the empty band is exactly what the chrome should spend first. Fixed by placing the drawer and the console as **one stack** rather than two boxes hugging one another: `--dock-top` is the whole stack's top, hugging the board but never letting the stack's *bottom* fall past the floor, and the console sits one `--dock-h` below it. In a 500x900 window the drawer went from covering 44px of board to covering 14. On the reference phone nothing moved - the floor was already winning there - which is the honest shape of the fix: it pays off in the letterboxed middle, not at the extremes.
- 2026-08-25: `--dock-h` is 0 whenever the drawer is not in the stack (landscape, or closed), and then `--dock-top` and `--panel-top` are the same number and the console lands exactly where it did before this step. So landscape still does not move a pixel, and the observer now watches the toolbar as well as the console - opening the drawer is a height change like any other.
- 2026-08-25: the drawer stays **above** the console rather than below it, which was the other reading of the complaint. Anchored as a stack, portrait always lands on the floor, so the console's bottom does not move when the drawer opens - the readout stays put and the menu is what appears. Below, the console would be shoved up 64px every time the menu opened.
- 2026-08-25: **the player settled the drawer question, and the answer was neither of the two the step had been arguing between.** Not "menu above the console" and not "menu below it in a shared stack": reading order straight down the screen - **board, console, build menu** - with the menu's row pinned to the floor beside the toggle that opens it, and the console hugging the board's underside above that row. The console's collapsed header therefore does *not* share the strip with the toggle, which the spec had said it would; the strip is the menu's row and the toggle's, and the console gets its own.
- 2026-08-25: the complaint underneath it was "o console troca de posição", and that is the part worth keeping. Both earlier shapes moved the console when the menu was toggled, because the menu's height was in the arithmetic only while it was open. So the strip is **reserved whether the menu is open or shut**: `--dock-h` is measured while the menu lies open and remembered, and the console is placed against a floor that already includes it. A readout that steps aside every time a menu opens is the readout apologising for the menu.
- 2026-08-25: which collapsed the step rather than growing it. `--dock-top` went away, the console went back to plain centred at its full 320px - it no longer has to dodge the toggle, since the toggle is a row below it - and the portrait override for the console is gone entirely. Three of the four boxes now say one thing each: the menu's row sits on the floor, the console sits under the board, and `--dock-h` is what keeps them from arguing.
- 2026-08-25: v1.4 steps 5 and 6 done together, since the second is the first one's dependent. `src/phone.ts` holds both: two buttons that are about the phone rather than about the game, right-anchored in the top band while the status glyphs keep the centre. The band is now a named row, `--hud-row-top`, because two boxes measure off it; the buttons bottom-align to it so 44px targets and 30px of text read as one line, with a clamp for windows that have no band to speak of. Manifest went `display: 'standalone'` to `'fullscreen'`, which drops the Android navigation bar in the installed app for one line and no code - and that bar is ~48px of an 81px band.
- 2026-08-25: **the lock needed a second check the spec did not anticipate.** "Rendered only where the API exists" is not enough: desktop Chrome defines `screen.orientation.lock()` and rejects every call, so a button gated on the method alone renders on every desktop and never works once. Gated on the method *and* a coarse pointer, which is the honest second half of the question - this control exists for a phone turning in a hand.
- 2026-08-25: a rejected lock **dims the button for a moment rather than disabling it.** Android rejects outside fullscreen, and pressing `FS` first is exactly what makes the next press work; a disabled button would hide that the door is there at all. Leaving fullscreen also drops an Android lock with it, so `fullscreenchange` clears the flag - otherwise the button sits there claiming a lock the platform already released.
- 2026-08-25: fullscreen renders on desktop too, against the spec's "desktop keeps the header it has today". That line was about the console's header not growing a fourth and fifth control, which still holds; the button is in the top band, the API is real there, and a fullscreen game on a desktop is not a mistake.
- 2026-08-25: **first phone playthrough of v1.4, and it came back okay with three changes.** All three are the player's, all three after actually holding it.
- 2026-08-25: the build menu **goes back to being a column in both orientations**, left of the console, growing upward out of its toggle and over the board. The horizontal row lasted exactly one milestone. The reasoning that produced it was not wrong - a turned board leaves a 3px side band and no column fits *in* it - it just answered the wrong question: the column overlaps the board's edge either way, so the only thing the row shape bought was a second layout to maintain. One shape that collapses beats two shapes that don't. It also cost more board than the column does: the column covers world rows 0-1, which carry no trace, while the row covered every row's near edge.
- 2026-08-25: **the toggle ships in landscape too**, which the spec never asked for - it had the menu permanently pinned there because the side band holds it. Playing it, "get this off the board" turned out not to be a portrait-only wish. So the menu is bottom-anchored above its toggle in both orientations rather than centred on the board's left edge, which also puts the dismissing gesture under the thumb in both. `--dock-h` and the whole turned-row block went away with it; the turned board now needs exactly one override, the console stepping right of the column.
- 2026-08-25: **the build kind is clearable.** Tapping the kind the console is already reading clears it - the same verb the board has used since v1.2, where tapping the selected thing again dismisses its reading - and the console falls back to the wave. Two things fall out that are worth more than the symmetry: with no kind picked, tapping an empty tile no longer spends Cycles, and there is finally a way to put the console back on the wave without selecting something else. `selectedTowerKind` became nullable and `panel.update` took the null with it; the placement preview and `placeTower` are guarded, and with nothing to build the console reads the wave on the board rather than the wave that is coming.
- 2026-08-25: still owed: the second phone pass on these three, and the installed-app half of verify. The three questions the milestone reserved for a playthrough - climbing enemies, whether the drawers earn their taps, a sideways trace - have a first answer of "okay", which is not the same as answered.
- 2026-08-25: **the toggle was stranded in the corner and the console had paid for it anyway.** Turned, the console stepped right by the *column's* width - the four buttons plus the menu box's padding and border, about 61px - while the toggle beside it is 44. So the console was visibly off centre and the `+` still sat 23px away from it, which is the price without the thing it was paid for. Fixed by making the column exactly one button wide when the board is turned: the menu's box drops its padding, background and border, and the buttons carry their own. That box exists to hold a column standing in a letterbox band; on the board it is a second frame around four framed buttons and only makes a wider thing to dodge.
- 2026-08-25: which lines the whole left edge up at 44px - toggle, column, and the width the console steps past are one number now - and puts the pair back near centre as a group: 8px of margin on one side and 12 on the other, on the reference phone. The four tower buttons stack straight above the toggle and over the board, which is what the expanded view is for.
- 2026-08-27: **the composer's first delivery arrived, and it is not what the brief asked for.** One mastered MP3 of 1:25, 48kHz joint stereo at 160kbps CBR, 1.66 MB - against a brief that asked for four WAV stems of 30 to 45 seconds and said in as many words not to send MP3 or a mastered track. Recorded as the fact it is rather than worked around, because it is what decides the shape of the milestone: v1.5 became **the desk and one track**, with the four layers pushed to a milestone of their own that cannot start until stems exist.
- 2026-08-27: the three numbers that planned the milestone are all measured off the file, not assumed. 84.7 seconds of stereo decodes to **~32 MB** of float32, which is the entire RAM budget the vault note derived for four layers, spent on one track; 1.66 MB is **66x** the whole PWA over the wire; and MP3 registers no encoder delay or padding, so the loop seam is not portable across browsers.
- 2026-08-27: **the track enters through a `MediaElementSource`, not a decoded buffer**, and that one decision is what the 32 MB bought back - `<audio>` streams, so the track stays compressed in memory. The bill is that a media element issues range requests and the service worker has to answer `206`, which is one Workbox plugin. Taken knowingly: a plugin is cheaper than 32 MB on a phone.
- 2026-08-27: the graph gets built anyway, with one file going through it. `<audio loop>` and three lines would make sound today, and that is exactly what the note refuses - the mute toggle the Idea Bank has wanted since before there was sound is a property of the bus, not a feature, and without a bus it is an `if` at every call site that the first sound added later will forget.
- 2026-08-27: **no start screen, against the study note's own advice.** `Áudio em PWA` argues audio forces a title screen because the context only unlocks in a gesture handler. It does not force one here: a `once` listener on the first pointerdown does it, with the audio button as the visible fallback. v1.4 already listed a start screen as out of scope, and letting it in through the audio door is scope arriving sideways. Costs a wave 1 that runs silent if the player touches nothing for five seconds. Revisited in the SFX milestone, which is when that silence stops being a curiosity.
- 2026-08-27: **encoding proceeds from the MP3 rather than waiting for the WAV**, which stacks a lossy generation the brief warned about. The reason it is affordable: the graph does not know where the file came from, so a re-encode later is a build-script change and zero code. Asking for the WAV in parallel rather than as a gate.
- 2026-08-27: the composer says the piece is written to loop, which turns the seam from a composition problem into a codec one - and opened the question worth more than any of this: **is there a ~40s sub-loop inside it?** A track built to loop is built from something that repeats. At 40 seconds a decoded buffer is ~15 MB, and then the loop is sample-accurate, the vault note's derived budget holds, and the crossfade machinery is never written at all. One question, and it is the difference between the provisional shape and the right one.
- 2026-08-27: the mute state lands in `localStorage`, which is the **first** in the project. Worth naming rather than sliding in - it is the seam local high scores plug into later.
- 2026-08-27: **v1.5 step 2 done - the desk exists and the game is still silent.** `src/audio.ts`: an `AudioContext` at `latencyHint: 'interactive'`, `musicBus` and `sfxBus` into a master gain into a `DynamicsCompressorNode` acting as a limiter, gains set in dB and moved with `setTargetAtTime`. Nothing is connected to either bus, which is the whole checkpoint.
- 2026-08-27: **deviation from the step as written**: the roadmap said the checkpoint was that "nothing else imports the module", and `main.ts` imports it anyway. The wording was wrong, not the code - a module nothing constructs never arms its unlock, never sees a `visibilitychange` and never reads `localStorage`, so the checkpoint would have been "it compiles" and the whole lifecycle would have gone unverified until the step that adds sound on top of it. Constructed and silent is a strictly stronger claim than absent, and it is the one that was actually checked in a browser. Roadmap reworded to match.
- 2026-08-27: **the gesture listeners are armed and disarmed, not fired `once`.** The obvious shape is a `once` listener that resumes the context and forgets itself. That shape strands the game permanently silent on Safari, where `interrupted` - a phone call, another app taking the session - drops a running context back to needing a fresh gesture, with the one listener already spent. So `statechange` is the switch: not running arms the listeners, running disarms them, and the arm/disarm cycle rides `visibilitychange` for free.
- 2026-08-27: `visibilitychange` only resumes a context **it suspended itself**. A context still waiting for its first gesture has to stay waiting, because resuming it there is a resume outside a gesture - which does not throw, it just does nothing, and then the next real tap finds the state already looking handled.
- 2026-08-27: verified in Chrome, which is the first time the extension has connected in three sessions of trying. Context comes up `suspended` at 48kHz - the same rate as the delivered track, so no resampling on decode - `musicBus` reads 0.25118 against the 0.25119 that -12 dB predicts, and a real click takes it to `running` while an `AnalyserNode` on the music bus reads a peak of exactly 0. Mute round-trips through `localStorage`, and faking `document.hidden` walks the context suspended and back to running.
- 2026-08-27: `dbToGain` is the module's one pure function and the only part testable off a browser, so it is the only part tested: decade steps, the -6 dB halving, a hard floor at -60 (an exponential approach to zero never arrives, and a bus at 1e-7 is a source still running), monotonicity, and the headroom claim from the brief as an assertion rather than a paragraph. **The first test written failed, and it was the test that was wrong** - half the amplitude is at -6.0206 dB, not -6, so a tolerance of 0.001 was asking the rule of thumb for a precision it does not have. Both bounds are asserted now, which writes down the ~0.2% of slop a mix argued in whole dB is carrying.
- 2026-08-27: not done here and owed by later steps: the level is `-12` by ear because there is still no ffmpeg on the machine to measure the delivered file with, and the `OfflineAudioContext` no-clip assertion needs an API Node does not have - it lands with the SFX milestone, where forty simultaneous kills make it a real question.
- 2026-08-27: **v1.5 step 1 done, and it overturned three things the design note asserted rather than confirming them.** ffmpeg extracted with the Windows `bsdtar` (libarchive reads 7z; there is no 7-Zip on this machine and no install was needed). Everything below is measured off the delivered file.
- 2026-08-27: **the delivery was not "mastered to streaming level", which the note had assumed and used to justify a level.** It reads -18.4 LUFS integrated over the loop region against a brief that asked for -18: the composer hit the loudness target exactly. The miss is the true peak, -0.1 dBTP against a -6 ask. Those two cannot both be fixed by a gain, because the delivery carries ~18 dB of crest where the brief's pair of numbers implied 12.
- 2026-08-27: loudness wins that argument on the evidence, not on preference: **0.013% of samples pass -3 dBFS**, which is eleven milliseconds across eighty-four seconds. Spending 6 dB of level to control eleven milliseconds throws away the half the composer got right. So `MUSIC_DB` is derived from the limiter instead - the smallest trim that keeps the music clear of its -3 dB threshold plus a decibel of margin - and it went **-12 to -4**. The by-ear guess was 8 dB off, which is about the size of error guessing at levels produces.
- 2026-08-27: **the track does not loop, and what it does instead is better.** An RMS profile shows a composed shape: intro at ~-25 dBFS for 19s, body at ~-15.5 dBFS for 55s, outro decaying to digital silence. Looping the file whole would replay the quiet intro every 85 seconds and drop out at 76. The body, though, is 55 seconds at LRA 1.1 LU - about as steady as material gets, and exactly what a loop wants.
- 2026-08-27: **and the grid is exact.** Refining the repeat period against long baselines puts it at 8.000s to within a sample (the k=2 and k=4 matches refined to 40.0000s and 56.0000s), which is four bars at **120.000 BPM**. Every section edge lands on it with no rounding: intro `[0, 20.000s)` is 10 bars, body `[20.000s, 76.000s)` is 28 bars, outro starts at 9.5 phrases. That is the difference between solving the loop problem and not having one.
- 2026-08-27: **which reversed the note's central architecture decision.** It said the track would enter through a `MediaElementSource` to keep the decoded megabytes off the heap, and the reasoning was sound. But `HTMLAudioElement.loop` restarts at zero and has no `loopStart`, so intro-then-loop through an element means polling `currentTime` and seeking - the drifting, gapping thing the whole note exists to avoid. `AudioBufferSourceNode` has `loopStart`/`loopEnd` and does it by sample for free. **The buffer is a requirement of the music, not a preference**, and it is also what the layers will need, so the provisional track and the real soundtrack now share a source node.
- 2026-08-27: that made RAM the binding constraint instead of something to dodge, which **decided mono against the vault note's own rule** ("mono for SFX, stereo for the track"). That rule was written assuming the track streams; once the buffer is forced, the note's other rule takes over - RAM limits, not the network - and stereo doubles it, 27.8 MB against 13.9. The material makes it cheap: L/R correlate at 0.90 and the side sits 12.7 dB under the mid, so there is width but not much, and most phones have one speaker. Revisit when the stems arrive and the pad wants its width back.
- 2026-08-27: bitrate chosen off the knee rather than off a table: measured against the source, 48k to 64k buys 2.3 dB, 64k to 80k buys 2.0, and 80k to 96k buys 0.6 for another 160 KB. Ships **Opus mono 80k at 799 KB**, trimmed to `[0, 76.000s)`, with no normalisation and no re-mastering - the file stays the master and the mix lives in the bus gain, so retuning the level is a constant rather than a re-encode.
- 2026-08-27: **the round trip verified in a browser, which is the check the vault note exists to demand.** Both encodes decode to exactly 3,648,000 samples - 76.000000s, delta zero - in Chrome and in ffmpeg. That is precisely what MP3 cannot promise: with nowhere standard to record encoder delay, each browser trims a different amount and `loopStart` stops being portable. Safari is still unverified, which promotes the AAC fallback from decorative to load-bearing: Safari's `decodeAudioData` has historically taken a narrower set of formats than its `<audio>` element, and the buffer path has no element to fall back to.
- 2026-08-27: the outro `[76s, 83.9s)` is cut rather than kept. A loop can never reach it, so carrying it in the buffer would cost RAM forever for something played once. Filed as the obvious source for an end-screen sting later, cut from the master as its own short file.
- 2026-08-27: **the sub-loop question to the composer is moot before it was sent.** It asked whether a ~40s repeating section existed inside the piece; the body is that section, it is 56s, and it was already on the grid. The WAV request still stands on its own.
- 2026-08-27: two steps got **smaller** when their assumptions were checked. `globIgnores` turned out to be unnecessary - `vite-plugin-pwa`'s default `globPatterns` covers `js,css,html,ico,png,svg` and never matched `.webm` or `.m4a`, which the build confirms at 7 precache entries and 54 KiB. And `RangeRequestsPlugin` went away with the media element, since `fetch` plus `decodeAudioData` issues no range request at all. Step 5 is now one runtime route.
- 2026-08-27: **the headroom test was rewritten, because measurement would have failed it while being right.** It asserted "the music bus sits at least 6 dB down", which was a restatement of the -12 guess; at the measured -4 it fails. The invariant that survives is the engineering one underneath: the music alone must stay clear of the limiter's threshold. That is a relation between three exported constants - the file's measured peak, the trim, and the threshold - so it still holds when any one of them is retuned.
- 2026-08-27: **v1.5 step 3 done - the soundtrack plays, and the loop is inaudible by measurement rather than by hope.** `src/music.ts`, kept separate from `audio.ts` because that module is the desk and this one is a source plugged into it; when the stems arrive this is the file that becomes the layer crossfader, and the desk will not have to change.
- 2026-08-27: the whole loop is `loop = true`, `loopStart = 20`, `loopEnd = 76`, which plays the intro once and then the body forever. Verified by rendering the seam in an `OfflineAudioContext` instead of waiting 76 seconds: the sample before the join reads -0.01668 and equals `buffer[76s - 1]`, the sample at the join reads -0.03093 and equals `buffer[20s]`, so the jump is exactly where it was told. **The step across the join is 0.02048 against a 0.22001 maximum elsewhere in the same four seconds** - five times smaller than the loudest ordinary transition in the music, and under its 99.9th percentile. There is no click, and that is a number rather than an opinion.
- 2026-08-27: nothing is fetched before the first gesture (checked: zero soundtrack entries in `performance.getEntriesByType('resource')` on a fresh load), and nothing is fetched at all while muted - a muted run should not spend 799 KB of someone's data on audio it will never hear. Unmuting later is a state change like any other, so a player who changes their mind gets the download then. Only the `.webm` is ever requested; the AAC fallback stays untouched unless the decode fails.
- 2026-08-27: **`MUSIC_DB` was wrong a third time, and the reason is the useful part.** -12 was by ear. -4 was derived from the source MP3's true peak of -0.1 dBTP. Both missed that **the graph never sees the file** - it sees what `decodeAudioData` returns, and a lossy codec does not preserve peaks. The delivered Opus decodes to a buffer peaking at **+2.52 dBFS, above full scale**, from a source measured at -0.1. Nothing was ever going to clip, because Web Audio is float end to end and the trim comes first; the reasoning was just calibrated against the wrong artifact. Now -7, which puts the music's loudest sample at a measured -4.48 dBFS with zero samples reaching the ceiling's knee.
- 2026-08-27: that costs loudness - the body lands near -24.4 LUFS where the brief asked for -18 - and the two cannot both be had, because the delivery carries ~20 dB of crest against the 12 its pair of numbers implied. Not clipping wins. Noted for when the WAV master arrives: **encode it with a few dB of headroom** and the overshoot stops existing.
- 2026-08-27: **the compressor at the end of the chain was the wrong node, and only measurement says so.** A 440Hz sine through the shipped settings - threshold -3, ratio 20, knee 0 - comes out **1.71 dB louder at every input level tested**, including -24 dBFS, twenty-one decibels below a threshold it is nowhere near. Chrome's `DynamicsCompressorNode` applies an implicit makeup gain and applies it always. A node that raises the peak is not a net; and the makeup is not in the spec, so compensating for it would mean calibrating against a number another browser is free to choose differently - **the same trap this project rejected MP3 over, one layer down**.
- 2026-08-27: replaced with a `WaveShaperNode` whose curve is written in `audio.ts`, so it is identical everywhere and cannot add gain. It is the *identity* below the knee, not approximately transparent - measured 0.00 dB of change at -24, -6 and -3 dBFS, against the compressor's +1.71 at each - and a tanh bend above it that asymptotes just under full scale. Because a shaper clamps input to [-1, 1] before the lookup, anything arriving over full scale is pinned to the curve's last value, so the ceiling is absolute rather than likely: +2.52 dBFS in comes out at -0.62. `4x` oversampling, because bending a waveform makes harmonics and harmonics past Nyquist fold back as aliasing.
- 2026-08-27: the whole body rendered through the shipped chain reads **peak -4.48 dBFS, RMS -22.62, zero clipped samples, zero samples past the knee** - matching the predicted -4.48 exactly. The ceiling is completely inert with music alone, which is the design intent: it is there for what the SFX will do later.
- 2026-08-27: **the headroom test has now been wrong twice in the same way**, so it was rewritten against constants rather than numbers. First it asserted "the bus sits at least 6 dB down", which was a by-ear guess restated as an assertion and would have failed the correct value; then it compared against the source file's peak instead of the decode's. It now asserts the relation - measured decoded peak, plus trim, stays under the ceiling's knee, with at least a decibel of margin - which survives any of the three being retuned. A second test records the surprise directly: this file decodes above full scale, and the trim is the only thing between that and the destination.
- 2026-08-27: **v1.5 step 4 done - the sound toggle, and `phone.ts` became `top-tools.ts` to hold it.** The rename is not tidying. That module described itself as "two buttons that are about the phone rather than about the game", and a sound toggle is not about the phone; the row is really the controls that belong to the session rather than to the run, set once and forgotten, which is why they sit in the hardest corner of a tall phone to hit by accident.
- 2026-08-27: **and the old shape had a bug the rename fixed.** Fullscreen and the orientation lock are both feature-detected, and on an iPhone neither exists - so `createPhoneControls` returned before creating the row at all. That would have put no sound button on the single platform where it is load-bearing: iOS is where the silent switch lives, and where a phone call drops the context into `interrupted`, which needs a fresh gesture to leave. The row now exists whenever any of the three will render, and **sound is last in it** so the one control that renders everywhere sits in the same place on every device.
- 2026-08-27: **`[x]` means sound is coming out, which is a stricter claim than "not muted"** - and getting that distinction right is most of what this step was. A context that has never seen a gesture, or that Safari interrupted, is silent while nothing is muted at all. Showing `[x]` there would be a lie, and it would hide the one control that can repair it. So the reading is `running && !muted`, and a press always does the obvious thing to the state being shown: unmute *and* resume when dark, mute when lit. That is what makes the button its own re-arm without a second control.
- 2026-08-27: which needed one addition to the desk: `setMuted` returns early when the state already matches, so unmuting an already-unmuted-but-suspended context did nothing. `AudioSystem.resume()` is now explicit and the button calls it unconditionally on the dark branch. Verified across every transition, including the one that matters: with the context suspended and nothing muted the button reads `[ ]`, and a press brings it back to `[x]` and running.
- 2026-08-27: **on a fresh load it reads `[ ]`, and that turned out to be a feature rather than a wart.** It is telling the truth about a context nobody has unlocked yet, which makes it **the nearest thing the game has to a start affordance** - in a milestone that deliberately refused to add a start screen. It also rights itself untouched, since any first tap anywhere resumes the context. One press on it from a cold load was verified to do the whole job at once: context running, track fetched, music playing.
- 2026-08-27: the toggle does real work beyond remembering a preference. **A run that starts muted never fetches the track**, so 799 KB of someone's data is not spent on audio they have said they do not want; verified by reloading with the flag set, tapping the board, and finding zero soundtrack entries in resource timing. Unmuting later is a state change like any other, and the download happens then.
- 2026-08-27: `aria-label` now speaks the word rather than the abbreviation - "Sound on", "Orientation lock off" - which is a small change to `makeButton` that improves all three at once. `SND` and not the `AUD` the design note proposed: three characters either way, and the row already speaks in the panel's abbreviations. No CSS at all: `.top-tools` was already a flex row of 44px buttons, and a third one just joins it.
- 2026-08-27: **the sound button's label became a pixel-drawn music note**, on the player's call, an hour after `SND` shipped. It is the one control in that row with a symbol everyone already knows - fullscreen and the orientation lock have none - so it is the one place a glyph beats a word. A beamed pair of eighth notes, drawn as whole pixels for the reason the HUD's heart is, and the heart's SVG builder moved out of `hud.ts` into `glyph.ts` the second time it was needed rather than being copied.
- 2026-08-27: the shape was chosen by rendering candidates at the size they would actually appear, not by reasoning. **A two-pixel beam was the first draft and reads top-heavy** at 15px - a staple rather than a note - so the beam is one pixel and the stems are five. Eight rows is not a style choice: a taller glyph pushes the button past the row's height, and `.top-tools` is a flex box, so it would have taken `FS` and `LOCK` up with it.
- 2026-08-27: **and then rasterising it found the real bug, which no screenshot would have shown.** At `1.25em` the eight rows landed in 15px, a scale of 1.875. Drawing the SVG into a canvas at its own size and dumping the alpha showed the one-pixel beam coming out two pixels thick and the stems alternating full and half coverage down their length. `shape-rendering: crispEdges` snaps edges; it cannot invent a grid. At a flat `16px` the scale is exactly 2.00 and every source pixel lands on a clean 2x2 block with no partial coverage anywhere. **This is the board renderer's own integer-scaling rule arriving in the chrome**, where nothing had been enforcing it - v1.1 chose integer scaling in device pixels for the board and the DOM glyphs never inherited the decision.
- 2026-08-27: the button's height did not move. It measures 45.6px with the glyph, which is what it measured with the word, and `FS` beside it is the same - so the row is untouched and the 44px minimum was never what was setting the height anyway.
- 2026-08-27: **found and deliberately not fixed: the HUD's heart has the same problem.** `0.8em` over six rows renders at 2.133x. The fix is one line (`height: 12px`) and the visible difference is 0.8px, but it is the HUD in an audio milestone and the player has been looking at that heart for three milestones. Recorded here rather than taken.
- 2026-08-27: **the three top-band buttons covered the wave indicator, and it was the third button that did it.** Reported from a phone, and the arithmetic says it was inevitable rather than unlucky: the status glyphs are centered and the tools are anchored right, so every control added to that row walks toward the readings. On the 390px reference phone the glyphs run 112 to 278 with `Lv N` at 230-265; two buttons put the row's left edge at 286 and cleared them, three put it at 238 and covered **27 of the wave indicator's 35 pixels**.
- 2026-08-27: **moving them up, which was the first thing asked for, would not have fixed it.** The top band is 81px and the portrait spec budgets it as one 44px row shared by the centered glyphs and the right-anchored tools. Lifting the tools out of the glyphs' line spends the band twice, and there is only ~25px of headroom before they sit under the notch in browser mode - not enough to clear a 46px row anyway.
- 2026-08-27: so they collapse instead, behind a gear, as a column growing **down** out of it - the mirror of the build menu, which grows up out of a toggle in the opposite corner. This fixes the collision at the root rather than by rearranging: **shut, the band's footprint is one 44px button on every device regardless of which controls render**, so it cannot come back the next time something is added to that corner. Measured on the reference phone: 60px of clearance from the glyphs shut, and open the column clears the core by 61px on the turned board.
- 2026-08-27: shut by default, unlike the build menu, which opens with the game. These are set once a session or not at all, and the menu's reason for standing open - that building three towers should not cost three reopenings - has no equivalent here. **It costs the sound button its second job**: shut, `SND` is no longer the visible `[ ]` that the previous entry called this game's only start affordance. It survives rather than dies, because any tap anywhere resumes the context - but it is weaker, and worth remembering if the start-screen question comes back with the SFX.
- 2026-08-27: `makeButton` grew a `kind`, because the two bracket vocabularies mean different things and this row now holds both. A `toggle` says `[x]`/`[ ]` and carries `aria-pressed`; a `drawer` says `[-]`/`[+]` and carries `aria-expanded`. The build menu and the console's collapse already own the second pair, so a drawer borrowing the first would be claiming to be a setting.
- 2026-08-27: **the gear could not be drawn at 8x8, and the player said so before the code did.** Eight by eight was chosen to match the note's exact 2x, and four hand-drawn candidates came out reading as a dumbbell, a flower, a spool and a cross. The reason is structural: a gear is a ring, a hub and teeth, and three concentric features do not fit in four pixels of radius.
- 2026-08-27: it is 16x16 at **1x** instead - still an integer scale, still sixteen device pixels tall, four times the detail. So the two glyphs in that row have different internal resolutions and the same footprint, which is the right way round: the note is a silhouette and wants chunk, the gear is a ring and wants pixels. **And it is generated rather than drawn** - a point is filled when it sits between the hub radius and an outer radius that alternates between body and tooth tip every eighth of a turn - because tuning a gear by hand means moving thirty pixels to change one radius. Radii picked by generating a spread and looking at them at sixteen pixels: deeper valleys make a more obvious gear right up until the ring breaks into arcs and starts reading as damage.
- 2026-08-27: a note on method, since it cost a wrong conclusion. A probe that cloned the live `.top-tools` out of a test iframe into the parent document rendered the drawer's open and shut states **swapped**, which looked like the drawer defaulting to open. It was not: reading the real thing in place - `hidden`, computed display, measured height, `aria-expanded` - showed shut on load and open after one click, exactly as written. Cross-document `cloneNode` was lying, and the fix was to stop cloning and measure the running app.
- 2026-08-28: **the WAV master arrived, and it is not the file the MP3 came from.** `RatTrap - TowerDefense.wav`, 84.58s, 48kHz/24-bit stereo, 23.3 MB. Same piece and same edit points - the body still starts at 19.99s and still decays from 76.03s, so `loopStart` and `loopEnd` did not move - but the audio underneath is a different render. Correlating the two amplitude envelopes gives 0.73, which says same arrangement and same rhythm; correlating them sample by sample over the body peaks at 0.43 at a lag that will not hold still between windows, which says re-rendered rather than re-exported. So nothing measured on the MP3 could be carried across, and everything was measured again.
- 2026-08-28: **the brief's two numbers stopped fighting, and that is the master's doing rather than any decision here.** The MP3 body read -17.45 LUFS at -0.06 dBTP - 17.4 dB of crest - against a brief asking -18 LUFS at ~-6 dBTP, which implies 12. Those cannot both be met by a gain, which is why `MUSIC_DB` cost 6 dB of loudness and the body landed near -24.4 LUFS. The WAV body reads **-13.59 LUFS at -1.03 dBTP: 12.6 dB of crest**, the ratio the brief actually described. One trim now hits both targets.
- 2026-08-28: **`MUSIC_DB` is -4 again, which was one of the wrong answers, arrived at correctly this time.** Peak: the AAC decode is the worse of the two at -0.84 dBFS, so -4 puts the music's loudest sample at **-4.90**, 1.9 dB clear of the ceiling's -3 knee. Loudness: the body lands at **-17.96 LUFS at the destination** against a brief asking -18. The "at the destination" is load-bearing and worth writing down - the buffer is mono, a mono buffer feeding a stereo destination is copied to both channels, and BS.1770 sums channel powers, so it gains 3.01 LU on the way. The file measures -16.97; what comes out measures -13.96 before the trim. Quoting the file's number here would have been off by 3 dB.
- 2026-08-28: **and the +2.52 dBFS decode overshoot was never the codec.** Yesterday's entry blamed lossy decoding and the conclusion it reached was right, but the explanation was wrong by 3 decibels. `ffmpeg -ac 1` does not average a stereo pair: its rematrix preserves **energy**, summing at 0.707 per channel rather than 0.5, so correlated material comes out 3 dB hot before an encoder sees it. Proof on the same MP3: `pan=mono|c0=0.5*c0+0.5*c1` peaks at **-0.42 dBFS** where `-ac 1` peaks at **+2.59**, and the shipped Opus read +2.52. The real overshoot is what is left: **0.2 dB**. Both encodes are now built with the explicit pan, the master's -1.10 dBFS survives as -0.903 through Opus and -0.843 through AAC, and nothing decodes above full scale any more.
- 2026-08-28: which turns a test the right way round. `tests/audio.test.ts` asserted that the decode overshoots full scale and only the trim saves it; it now asserts the master has headroom of its own. That is the more durable invariant - a master needing `MUSIC_DB` to stay under full scale is one where the volume slider this milestone deliberately did not build could put it back over. The headroom test itself did not change at all, which is the argument for having written it against constants rather than numbers: both of its inputs moved and it still passes.
- 2026-08-28: **the loop seam got measurably better, and measuring it turned up that the old one was worse than anyone had said.** Yesterday's check was that the join lands on the right sample and that the step across it is small, which it was. The check not done was whether the join sounds like a bar line. Spectral flux across the seam against the same measure at all 27 bar lines inside the body: the MP3 encode read **3.33 where the worst ordinary bar line read 2.47** - the seam was the single largest spectral event in the loop, quieter than none of them. The WAV encode reads **1.54 against a bar-line median of 1.84**, quieter than 25 of 27. The join is now less of an event than an average bar.
- 2026-08-28: the swap itself cost two constants, two files in `public/`, and no code - which is exactly the bet the encode step made when it proceeded from the MP3 rather than waiting for the WAV. Verified in Chrome on a production build: both files decode to exactly 3,648,000 samples at 76.000000s, peaking where ffmpeg says they peak to the hundredth of a decibel, and one real click fetches `soundtrack.webm` and only that one.
- 2026-08-28: **found while verifying, and not a bug in this change: a stale service worker served the previous build's JS for two page loads**, which showed up as the top-tools drawer missing from the DOM entirely. Unregistering it and clearing the caches fixed it. Worth remembering before the PWA delivery step goes anywhere near a `CacheFirst` route for the audio - the trap that step already lists, that `soundtrack.webm` carries no hash in its name, is the same failure with a longer lifetime.
- 2026-08-28: the two commands, since they are the whole build until that script exists. Trim to the loop, downmix explicitly, no normalisation - the file stays the master and the mix lives in the bus gain.

```sh
ffmpeg -i "RatTrap - TowerDefense.wav" -t 76 -af "pan=mono|c0=0.5*c0+0.5*c1"   -c:a libopus -b:a 80k -vbr on -application audio -map_metadata -1 public/soundtrack.webm
ffmpeg -i "RatTrap - TowerDefense.wav" -t 76 -af "pan=mono|c0=0.5*c0+0.5*c1"   -c:a aac -b:a 128k -map_metadata -1 public/soundtrack.m4a
```

- 2026-08-28: recorded as an owed cleanup rather than done here. The two encodes are the only copies of any of this in the repo, so **rebuilding them is a manual ffmpeg run against a file on one machine** and the commands live in this log rather than in a script. That was fine while the source was a placeholder; with a real master and four stems still to come it is not, and the script is also where the mono pan stops being a thing anyone can forget.
- 2026-08-28: **o jogador ouviu um buraco em 40s, e ele existe e é novo neste render.** Ouvido primeiro, medido depois: a energia abaixo de 120 Hz cai de -17 para -25 dB entre 39,85s e 40,65s e sobe de volta em meio segundo. A mesma janela no MP3 não tem buraco - fica entre -20 e -22 o tempo todo. Cai em cima da divisa de compasso dos 40,000s, então provavelmente é arranjo e não acidente, mas é a única coisa naquela janela que separa os dois renders, o que faz dele o candidato certo para o que foi ouvido. Vai como pergunta ao compositor, não como correção.
- 2026-08-28: **e a resposta para "sem stems dá para mixar?" é não, com um exemplo agora.** Um bounce só dá ao jogo exatamente um controle, o ganho da faixa inteira; toda decisão de textura está assada no arquivo. O buraco dos 40s é a demonstração: não dá para suavizar, mover nem cruzar deste lado. Com os quatro stems do brief a mesma entrada seria uma rampa de ganho que o jogo controla, que é a diferença entre trilha adaptativa e uma faixa com botão de volume.
- 2026-08-28: **o retorno da segunda entrega foi escrito duas vezes, e a segunda é um quarto do tamanho.** O primeiro rascunho levava cinco assuntos: a correção dos 3 dB do downmix, o LUFS que finalmente bate, a medição da emenda, o render ser outro, e os stems. Cinco assuntos numa mensagem para uma pessoa é uma resposta para nenhum deles. **O único pedido que importa é o dos stems**, então ele virou o corpo, com a pergunta dos 40s antes por ser a do ouvido e não a da medição, e crédito e licença como última linha. O resto ficou escrito no brief e vai por cima se ele perguntar.
- 2026-08-28: o que o corte deixou de fora vale nomear, porque uma coisa ali é dívida e não notícia. A mensagem anterior pediu 3 a 6 dB de teto justificando com um estouro de +2,52 dBFS atribuído ao codec, e 3 daqueles decibéis eram o downmix `-ac 1` deste lado. O pedido continuava certo e a justificativa estava inflada. **A correção não some por ser cortada** - fica registrada e é a primeira coisa a dizer se ele perguntar por que o teto foi pedido, porque pedir a alguém que mude o jeito de bouncear com um número errado é caro em confiança e barato de desfazer.
- 2026-08-28: crédito e licença continuam abertos, e agora atravessaram duas entregas. O checklist do brief marca só o WAV.
