# Log 2026-09

Part of [Log](./Log.md). Oldest first.

- 2026-09-11: **o primo respondeu, e as duas metades vieram juntas.** Crédito: `Composição: Leo Benedetti`, `Produção: Ancestor SoundWorks`, com o site do estúdio. Licença: **não existe licença** - a trilha é favor, usada com permissão, sem nada escrito. Isso é resposta e não ausência de resposta, e o que ela decide é o texto do README em vez de um arquivo: repo público sem `LICENSE` lê como "pode pegar" para muita gente, e a música é a única coisa aqui que não é do projeto para dar. Então o áudio de `public/` é recortado em palavras - não é asset livre e não anda junto com o código.
- 2026-09-11: **a dívida foi paga antes da superfície contra a qual estava arquivada, e de propósito.** O [Roadmap](../Roadmap.md) punha o crédito no passo "o que se muda para dentro" do v1.7, com o argumento de que não existia no jogo nenhum lugar onde um crédito coubesse. **Esse argumento venceu em 30/08**, quando o `ABOUT` entrou como quinto modo do console; de lá para cá o que faltava era só a resposta. Segurar a linha até a shell - que está a dois passos - seria dever mais tempo por um motivo que já não existe. O passo do v1.7 continua dono de *mudar* a linha de lugar, porque ela é parte da leitura e não do painel.
- 2026-09-11: **no jogo é uma linha só, e ela nomeia o estúdio e não a pessoa - chamada do jogador.** O `ABOUT` tem cinco linhas e gasta três, então cabia nomear os dois; o argumento que decidiu foi outro: **o link e o nome têm que ser a mesma coisa.** Uma linha que nomeia o compositor e leva para o site do estúdio lê como dois créditos vestindo uma linha só. Fica `MUSIC .... ANCESTOR SOUNDWORKS` abrindo o site, e o README carrega os dois nomes, porque lá cabe dizer quem fez o quê.
- 2026-09-11: dois detalhes que são construção e não enfeite. `<a target="_blank">`, porque **PWA instalado que navega para fora de si não tem botão de voltar** - a run iria embora por causa de um crédito. E a linha mora **fora do grid de stats**, largura inteira: "ANCESTOR SOUNDWORKS" é mais largo que uma das duas colunas de 118px do grid, e é o único item do console que é controle em vez de leitura, então estar fora dele é honesto além de necessário. Reaproveita a classe `.stat`, então o dot leader é o mesmo de todas as outras linhas, e o sublinhado vai só no valor - num readout onde todo valor já é texto verde, não sobrou outro jeito de dizer "esse abre". O `href` é o `www`, que é onde o apex redireciona.
- 2026-09-11: `tsc` limpo e os 44 testes passando, e nenhum deles cobre isso - é DOM e é uma linha de texto. **A verificação visual fica devendo ao celular**, como toda verificação deste projeto: a extensão do Chrome nunca conectou nesta máquina.
