# Log 2026-09

Part of [Log](./Log.md). Oldest first.

- 2026-09-11: **o primo respondeu, e as duas metades vieram juntas.** Crédito: `Composição: Leo Benedetti`, `Produção: Ancestor SoundWorks`, com o site do estúdio. Licença: **não existe licença** - a trilha é favor, usada com permissão, sem nada escrito. Isso é resposta e não ausência de resposta, e o que ela decide é o texto do README em vez de um arquivo: repo público sem `LICENSE` lê como "pode pegar" para muita gente, e a música é a única coisa aqui que não é do projeto para dar. Então o áudio de `public/` é recortado em palavras - não é asset livre e não anda junto com o código.
- 2026-09-11: **a dívida foi paga antes da superfície contra a qual estava arquivada, e de propósito.** O [Roadmap](../Roadmap.md) punha o crédito no passo "o que se muda para dentro" do v1.7, com o argumento de que não existia no jogo nenhum lugar onde um crédito coubesse. **Esse argumento venceu em 30/08**, quando o `ABOUT` entrou como quinto modo do console; de lá para cá o que faltava era só a resposta. Segurar a linha até a shell - que está a dois passos - seria dever mais tempo por um motivo que já não existe. O passo do v1.7 continua dono de *mudar* a linha de lugar, porque ela é parte da leitura e não do painel.
- 2026-09-11: **no jogo é uma linha só, e ela nomeia o estúdio e não a pessoa - chamada do jogador.** O `ABOUT` tem cinco linhas e gasta três, então cabia nomear os dois; o argumento que decidiu foi outro: **o link e o nome têm que ser a mesma coisa.** Uma linha que nomeia o compositor e leva para o site do estúdio lê como dois créditos vestindo uma linha só. Fica `MUSIC .... ANCESTOR SOUNDWORKS` abrindo o site, e o README carrega os dois nomes, porque lá cabe dizer quem fez o quê.
- 2026-09-11: dois detalhes que são construção e não enfeite. `<a target="_blank">`, porque **PWA instalado que navega para fora de si não tem botão de voltar** - a run iria embora por causa de um crédito. E a linha mora **fora do grid de stats**, largura inteira: "ANCESTOR SOUNDWORKS" é mais largo que uma das duas colunas de 118px do grid, e é o único item do console que é controle em vez de leitura, então estar fora dele é honesto além de necessário. Reaproveita a classe `.stat`, então o dot leader é o mesmo de todas as outras linhas, e o sublinhado vai só no valor - num readout onde todo valor já é texto verde, não sobrou outro jeito de dizer "esse abre". O `href` é o `www`, que é onde o apex redireciona.
- 2026-09-11: `tsc` limpo e os 44 testes passando, e nenhum deles cobre isso - é DOM e é uma linha de texto. **A verificação visual fica devendo ao celular**, como toda verificação deste projeto: a extensão do Chrome nunca conectou nesta máquina.
- 2026-09-11: **verify do jogador no celular: ficou bom, nada voltou para a mesa.** Voltou o veredito e não as três perguntas uma a uma - o sublinhado como afordância de link, a altura da linha como alvo de toque e a volta da aba nova para dentro da run ficam confirmadas em bloco. Quarta verificação deste projeto feita no aparelho e não pela extensão, que segue sem conectar.
- 2026-09-11: **v1.7 passo 1 - a run virou estado, e o estado é o `null`.** `let state: GameState | null`, e **de propósito não existe um `mode` do lado**. Um booleano que precisa concordar com uma run anulável é duas fontes de verdade para um fato só, e o jeito como isso derrapa é um dos dois ser setado num lugar em que o outro não é - que é exatamente a família de bug sobre a qual este projeto já tem nota. O `resetGame()` virou `startRun()`: mesmo código, nome que parou de supor que sempre existe uma run rodando, e é nele que o descritor de run cai no passo 2.
- 2026-09-11: **a anulabilidade custou umas 25 chamadas e comprou um bug de verdade.** O `spawnArmed` nunca esteve no reset: arma o port, perde antes de tocar nele, toca para reiniciar - e o toque que reinicia sai da função antes da linha que desarma. O board novo abre com as setas acesas. **E não é cosmético**, que foi a minha primeira leitura e estava errada: o tick que limpa arme velho só roda quando não há wave para chamar, e run nova abre *dentro* de uma contagem, então a flag atravessa. O primeiro toque do jogador no port chamaria a wave 1 adiantada sem nenhum dos dois toques de confirmação que o port existe para exigir. Achado escrevendo a lista do que uma run possui, que é o argumento do passo inteiro.
- 2026-09-11: o que fica escondido na shell é o que fala do board - HUD, console, menu de build - e **a coluna da engrenagem fica**, porque som, fullscreen e a build do ABOUT continuam verdade sem run nenhuma. O `clearDisplay()` saiu do `present()` para o `canvas.ts` exportado: frame sem mundo para blitar é só o preto, em vez do último frame de uma run que acabou - que seria a mentira mais convincente que este jogo sabe contar.
- 2026-09-11: **`?shell` entrou como andaime com prazo de validade.** Um estado que nada ainda sabe mostrar é um estado que ninguém consegue olhar, e "não muda nada" verificado só por `tsc` é a versão fraca do checkpoint. Query string e não global de DEV pelo motivo de sempre neste projeto: a verificação acontece no celular, e no celular dá para digitar uma URL e não dá para chamar `window.startRun()`. O `?gallery` é o precedente. O passo 2 põe botão de start na shell e aposenta o parâmetro.
- 2026-09-11: duas coisas que o passo **não** resolveu e que são do passo 2, escritas antes de alguém tropeçar. O loop continua rodando na shell e nada é passado por ele - de propósito, porque parar e religar o rAF é mais caro que um `return` e a shell vai querer animar alguma coisa. E **o teclado continua respondendo sem run**: espaço pausa, `1-4` escolhe torre, `F` troca velocidade. Hoje nada disso é visível (não se desenha nada e o menu está escondido) e o `startRun()` limpa a pausa, então é inerte - mas inerte é diferente de decidido, e quem decide é a tela que vai existir.
- 2026-09-11: `tsc` limpo, 44 testes passando, build ok - e nenhum dos três diz o que o checkpoint pede, porque o checkpoint é "nada mudou jogando". Isso é olho no celular, como sempre.
- 2026-09-11: **v1.7 passo 2 - a shell existe e o boot para nela.** `src/shell.ts`, DOM como todo chrome deste projeto, na mesma voz do console: prompt `>`, nome, cursor piscando. É **estado e não splash** - nada ali tem tempo e nada se dispensa sozinho; o app fica na shell até uma run começar.
- 2026-09-11: **o descritor de run ganhou módulo próprio (`runs.ts`) porque run não é mapa.** Endless é outro *tipo* de run no mesmo mapa e dificuldade é um botão antes da run - os dois pousam ao lado do mapa naquele arquivo, e nenhum dos dois caberia como campo do `LevelData`. O `id` já existe sem ser mostrado: recorde é afirmação sobre uma run, então tem que ter por onde nomear qual.
- 2026-09-11: **lista de um vira botão e não menu**, e o desenho inteiro do picker coube numa expressão: `runs.length === 1 ? 'START' : run.name`, em cima de um loop que já constrói lista. O nome da entrada só é impresso no dia em que existe algo entre o que escolher, que é exatamente o que o roadmap argumentou como não-objetivo. A segunda entrada é uma linha no `runs.ts`.
- 2026-09-11: **a camada da shell não pega ponteiro nenhum, e isso é achado e não detalhe.** Ninguém neste projeto usa `z-index`, então camada anexada por último pinta por cima de tudo - inclusive da coluna da engrenagem, que é sobre a sessão e não sobre a run e precisa continuar viva sem run na tela. `pointer-events: none` na camada e `auto` só na caixa do meio resolve sem inventar uma ordem de empilhamento que o projeto até hoje não precisou ter.
- 2026-09-11: o `startRun()` passou a receber o descritor, e o board e o `spawnTile` são re-derivados **só quando o mapa sob a run muda**. É condição e não suposição: hoje nunca dispara, porque `RUNS` tem uma entrada, e é por isso que a segunda entrada não pede mudança aqui. O que fica devendo e está escrito: **o viewport continua dimensionado pelo mapa do boot** - um segundo mapa de outro tamanho é a única coisa que vai precisar reconstruir ele, e isso é do v1.8.
- 2026-09-11: duas coisas caíram do passo sem estar nele. **Os atalhos de teclado agora exigem uma run**, e isso não é arrumação: sem o `return`, o handler continuaria dando `preventDefault` no Espaço e roubaria a ativação do botão de start que acabou de receber foco. E o botão **recebe foco** quando a shell aparece - o teclado ganha o mesmo movimento único que o dedo tem, e a única coisa na tela fica visivelmente sendo a coisa a apertar, que é a diferença entre uma tela que começa e uma tela que parou.
- 2026-09-11: `?shell` aposentado no mesmo passo que prometeu aposentá-lo, que é o ponto de andaime ter prazo escrito junto.
- 2026-09-11: o que **não** foi feito e continua no roadmap: a run ainda termina no board com tap-to-restart (passo 3), o `RELOAD` do update ainda mora no console (passo 6), e ABOUT e o crédito seguem na engrenagem (passo 5). **A pergunta que só o celular responde** é a do passo de verify e é a que decide se este milestone acertou: a shell lê como o jogo começando, ou como barreira na frente dele? Um visitante de portfólio dá trinta segundos, e este passo acabou de gastar um toque deles.
- 2026-09-11: **v1.7 passo 3 - a run termina na shell, e o veredito fica no board.** `TAP TO RESTART` virou `TAP TO RETURN` e o toque vai para a shell em vez de cair numa run nova. O veredito **não** se muda junto: ele fica na tela de fim, desenhada no canvas por cima do board em que foi ganho, porque o board é metade do que um veredito quer dizer. A shell ainda não tem o que dizer sobre uma run terminada - isso é o passo de recordes, que é o próximo.
- 2026-09-11: o `endRun()` é só largar o estado, e a única outra linha dele é a seleção - que **não é reset, é liberação**: ela aponta para uma torre de uma run que não existe mais, e run largada que alguém ainda aponta não foi largada. Todo o resto do que descreve uma run é reestabelecido pelo `startRun()`, então não há nada aqui para zerar.
- 2026-09-11: **o passo apagou estado em vez de somar.** O `currentRun` existia só para o tap-to-restart repetir o mesmo descritor; sem o restart, ficou sem leitor nenhum. Deletado em vez de guardado para o passo de recordes - campo mantido para um leitor futuro é exatamente como estado morto se acumula, e quando os recordes precisarem dele vão trazê-lo de volta com um uso de verdade junto.
- 2026-09-11: o que isso cobra do jogador é **um toque a mais para jogar de novo**, e essa é a aposta do milestone inteiro em uma frase. A pergunta que o celular responde não é se o toque incomoda: é se cair numa tela depois de perder lê como ciclo de tentar de novo ou como ser posto para fora do jogo.
- 2026-09-12: **v1.7 passo 4 - recordes, e a identidade da curva sai da curva.** `CURVE_ID` é FNV-1a de `JSON.stringify(waves)`, gravado junto com o conjunto. Conjunto cuja curva não é esta **é descartado e não migrado**: não está desatualizado, é sobre outro jogo. A curva se mexeu duas vezes num dia só no v1.6, e uma constante que alguém precisa lembrar de subir é exatamente o tipo de coisa que este projeto já tem nota explicando como perdeu.
- 2026-09-12: **duas definições foram herdadas e não inventadas, e isso é o mais importante do passo.** Leak é HP perdido do core, porque é assim que o harness conta desde o v1.3 - contar diferente daria dois números com o mesmo nome. E o relógio é **tempo simulado**: tick é `TICK_MS` independente do botão de velocidade, então 2x não compra recorde nenhum e a única alavanca no tempo é a chamada antecipada, que é a mecânica que existe exatamente para essa troca. De brinde, é o mesmo número que o `balance.ts` imprime como duração de run, então recorde e leitura do harness são comparáveis em vez de parecidos.
- 2026-09-12: a forma do readout é argumento e não layout. `FURTHEST` é a única afirmação que uma run que nunca ganhou consegue fazer; depois do primeiro clear ela leria 15/15 para sempre, então `FASTEST` e `CLEANEST` **ocupam o lugar dela** em vez de sentar embaixo. Visitante de primeira viagem não vê bloco nenhum: título e botão.
- 2026-09-12: **a run é arquivada no tick em que termina, não no toque que sai dela.** Quem fecha a aba no veredito continua tendo feito o recorde que fez - é fato sobre a run e não sobre ter ficado olhando. Detecção por borda (`playing` antes, não-`playing` depois), que é o jeito de não precisar de uma flag "já gravei" que alguém tem que manter em dia.
- 2026-09-12: o `NEW` âmbar reaproveita o `.stat-next` do console, que quer dizer a mesma coisa do lado de um recorde e do lado de um preview de upgrade: **isso aqui acabou de se mexer.** Some quando a shell some, porque recorde é permanente e ter acabado de fazer não é. E é a primeira coisa do milestone que faz voltar para a shell ser chegada em vez de despejo - meia resposta, por construção, para a pergunta que o passo 3 deixou aberta.
- 2026-09-12: `tests/records.test.ts` com `localStorage` stubado, e **os gates são das regras e não da aritmética**, no espírito do `balance.test.ts`. Os dois que valem: curva estranha é descartada, e **derrota nunca leva recorde de clear** - que é a armadilha de verdade, porque uma run que morre na wave 3 é mais rápida e vazou menos que qualquer run que chegou ao fim. Mais: run pior não escreve byte nenhum no storage, e storage corrompido lê como sem recorde em vez de como crash. 52 testes no total agora.
- 2026-09-12: **v1.7 passo 5 - o ABOUT saiu da engrenagem e do console, e virou o rodapé da shell.** A pergunta que o passo carregava era se crédito continua lendo como crédito numa tela que também tem botão de start. A resposta que a mudança achou é **rodapé e não mais linhas**: a caixa do meio é o caminho para dentro do jogo, e crédito mais carimbo de build empilhados em cima do START transformariam o caminho para dentro numa página para ler. Na borda de baixo é onde tela de título sempre pôs isso.
- 2026-09-12: são três linhas lá embaixo - o reload quando existe build esperando, `MUSIC BY ANCESTOR SOUNDWORKS`, e `BUILD <sha> · <data>` - e **as três estavam antes atrás de uma engrenagem e depois de uma gaveta.** Agora estão numa tela que o jogador atravessa de qualquer jeito. O reload fica em cima das outras duas de propósito: é a única coisa ali que age, então fica perto do botão e não no rodapé do rodapé. Ordem mexida no DOM e não com `order` do CSS, senão ordem de leitura e ordem de tab discordam.
- 2026-09-12: **o console voltou a ter quatro modos e a engrenagem voltou a ser só settings.** `setAbout`, `renderAbout`, a linha do crédito, o botão de reload e o par `aboutOpen`/`aboutUpdateReady` foram deletados, não estacionados - e o `main.ts` perdeu o `syncAbout` junto. Aquilo era "o ABOUT está aberto" cruzado com "tem build esperando", e só a segunda metade sempre foi fato sobre o mundo; a primeira era estado de UI que dois módulos tinham que manter em dia entre si.
- 2026-09-12: **o passo 6 fechou junto, e forçou a decisão que o roadmap tinha deixado em aberto: oferecer na costura livre, não aplicar nela.** O console não guarda `RELOAD` nenhum, então no meio da run a resposta é o "agora não" honesto - não existe mais superfície nenhuma durante a run que fale de update. E a shell **não** se recarrega sozinha quando uma build chega: página que recarrega debaixo de alguém é exatamente o que o `registerType: prompt` foi escolhido para impedir, e os dois segundos que isso economizaria valem menos do que não fazer isso com quem está lendo o recorde que acabou de fazer. Sobra uma cobertura de três lados e nenhum deles é armadilha: fecha o app e o worker em espera assume sozinho, termina uma run e a oferta está ali embaixo do start, ou nunca repara e segue jogando a build que tem.
- 2026-09-12: de brinde, um comentário que estava mentindo foi corrigido no `top-tools.ts`. Ele dizia que o early return tinha acabado "porque o ABOUT renderiza em todo aparelho" - com o ABOUT fora, quem sustenta isso agora é o botão de som, que é o único dos quatro que não é feature-detected. Fullscreen e lock não existem num iPhone; áudio existe.
- 2026-09-12: **v1.7 fechado - verify do jogador no celular, e nada voltou para a mesa.** O veredito veio em bloco e não como três respostas, e isso vale registrar exatamente assim: a shell não lê como barreira, terminar dentro dela não lê como despejo, e **a terceira pergunta - se recorde diz alguma coisa com curva fixa - não foi respondida sozinha.** Então ela fica como *não contrariada* e não como confirmada, que é diferente, e é o que observar na próxima vez que alguém jogar isso frio. Era justamente a que a nota tinha marcado como a única que podia voltar "não"; se voltar um dia, a resposta é endless e não outro recorde.
- 2026-09-12: o milestone inteiro saiu numa sessão, sete passos, e o que o tornou barato foi o que a [Before the Run](../Idea%20Bank/Before%20the%20Run.md) já dizia: nenhum dos cinco itens travados queria uma tela de título, todos queriam **existir um antes**. Construído o antes, três deles caíram como consequência e não como trabalho - recorde tem onde ser lido, crédito tem onde morar, e a build em espera tem costura. O quarto (mapa 2) fica a uma linha de dado de distância, e o quinto (endless) continua sendo escolha e não bloqueio.
- 2026-09-12: **próximo é o mapa 2 com o seletor**, que é a ordem que a sessão de 31/08 argumentou e que o v1.7 não mexeu. É o primeiro conteúdo deste projeto em vez do primeiro sistema, e é a segunda entrada que faz a lista virar lista - o `runs.ts` já lê uma, e a shell já imprime nomes em vez de um botão de start no instante em que houver mais de um para imprimir. Sem passos escritos ainda: o que o passo 2 do v1.7 deixou anotado como dívida real é que **o viewport ainda é dimensionado pelo mapa do boot**, então um mapa de outro tamanho é a única coisa que vai precisar reconstruir ele.
- 2026-09-12: **a terceira pergunta do verify voltou, e a resposta trouxe a condição junto: "agora que só tem uma fase, ficou bom o recorde".** Isso responde e limita na mesma frase - o recorde é bom *porque* existe um board só. Fica registrado como resposta e não mais como não-contrariado.
- 2026-09-12: e a condição achou uma armadilha que estava montada para o v1.8. **A curva é global e o mapa não é**: `waves` mora no `wave.ts` e o `RunDescriptor` carrega só o mapa, então dois mapas dividem o mesmo `CURVE_ID`. Com o recorde arquivado só sob a curva, o `FASTEST` dos dois boards cairia na mesma linha, e o que ela diria é qual dos dois é mais fácil - **exatamente a classe de erro que o hash foi construído para impedir, um andar acima: recorde é afirmação sobre uma curva *e* sobre um board.** O conserto é uma linha (o `id` do descritor entra na chave, e ele já existe no `runs.ts` documentado como "é sob isto que um recorde será arquivado"), e o certo é fazê-lo no passo que cria a segunda entrada, não agora: com um mapa só não dá para verificar que funcionou, e mudar a chave hoje só apagaria o recorde que o jogador acabou de aprovar.
- 2026-09-12: **v1.8 escolhido e planejado antes de qualquer código, na chamada do jogador de definir a versão primeiro** - que é a convenção do projeto e que eu ia atropelar. O segundo board, com eixo e não com rabisco novo: **o traçado dobra de volta**, com a perna de volta a duas linhas da perna de ida, então uma torre no corredor entre elas cobre as duas. A pergunta vira *onde* em vez de *quantas*.
- 2026-09-12: e não é ideia nova, é uma dica velha virando o ponto. **O mapa 1 já faz isso num tile só**: as pernas de y=2 e y=4 se encontram na coluna x=4, e o `VETERAN_BUILDS` do `balance.ts` explora isso desde o v1.3 com dois Firewall Node na linha 3. O board novo abre essa sobreposição de uma coluna para cinco.
- 2026-09-12: **a consequência que apareceu no planejamento e que muda o tamanho do milestone: dobra custa comprimento.** Num board de 16 de largura, qualquer volta atravessa a horizontal duas ou três vezes em vez de uma, então o traçado sai dos 19 tiles do mapa 1 para uns 27. Com curva fixa, **comprimento de traçado é dificuldade** - 40% mais trace é 40% mais tempo dentro do alcance - então o segundo board nasce mais fácil que o primeiro sem ninguém ter pedido. Isso não se resolve no olho: o `runMargin` existe exatamente para isso, e apontar o harness para o board novo virou passo do milestone em vez de nota de rodapé. Os waypoints exatos ficaram **fora do plano de propósito**; o plano fixa as restrições e os números saem do instrumento.
- 2026-09-12: não-objetivo escrito com o motivo junto: **segunda curva de waves, não.** A curva é global e continua - se o board precisar de outra pressão, a resposta é a forma dele e não uma segunda tabela, porque duas curvas dobrariam toda pergunta de tuning que o harness responde e deixariam o hash do recorde ambíguo na mesma semana em que ele deixou de ser.
- 2026-09-12: **o v1.8 foi interrompido no primeiro passo, a pedido do jogador, para ranquear o [Idea Bank](../Idea%20Bank/Idea%20Bank.md)** - que abre dizendo "otherwise unprioritized" desde 12/07 e tinha uns trinta itens abertos entre a página principal, as direções não agendadas da [Difficulty](../Idea%20Bank/Difficulty.md) e o login da [Before the Run](../Idea%20Bank/Before%20the%20Run.md). Sessão sem código de propósito, como a de 31/08.
- 2026-09-12: **o critério é do jogador e é o que fez o ranking sair: quatro eixos ao mesmo tempo**, e não um. O que outros itens esperam, o que o jogador sente, profundidade de aprendizado, e o buraco de design mais óbvio - com a regra de que **o topo é onde os eixos concordam**. Item que vence em três passa na frente de item que vence forte em um, e é isso que finalmente desempata a briga mais velha da página, juice contra módulo de engenharia. Pedir um eixo só teria produzido quatro listas diferentes e nenhuma decisão.
- 2026-09-12: **a primeira leitura nova, do jogador: o relatório pós-run é instrumento e não só legibilidade.** A [Difficulty](../Idea%20Bank/Difficulty.md) já escreve as duas metades sem juntá-las - "o harness e os jogadores estão medindo coisas diferentes" e "toda run simulada é piso porque o harness nunca usa Overclock". O relatório mede a run que *usa* Overclock e *muda de ideia*, que é a descrição literal do layout adaptativo que aquela nota pediu e ninguém agendou. Isso o move para a frente da pergunta do Roster em vez de para o lado dela: não se escolhe entre quinta torre e tier 4 antes de medir.
- 2026-09-12: **a segunda, também do jogador, e ela encurtou a lista: o replay determinístico é a terceira ponta do mesmo instrumento.** O relatório é agregado ("a wave 7 custou 2 de HP"); o log é a run inteira, então dá para rodar uma run humana *dentro* do `balance.ts` e fechar o buraco de forma literal em vez de por aproximação. Dois itens que eu tinha ranqueado separados (1 e 9) eram o mesmo item, e a fila caiu de sete slots para seis - que é o resultado certo quando isso acontece.
- 2026-09-12: três consequências disso, e duas não estavam em nenhuma nota. O **layout adaptativo** perde a justificativa principal, porque era proxy de "uma linha que muda de ideia" e run humana gravada não é proxy - mas sobrevive como *gate de regressão*, porque **replay é escopado à curva que o produziu**, que é a lição do `CURVE_ID` do v1.7 um andar acima: retuna a curva e todo replay guardado passa a ser sobre outro jogo. O **gate de determinismo vai primeiro** dentro do item, porque o item inteiro supõe que jogo e harness são a mesma simulação, e hoje isso é afirmação e não teste - se divergiram em silêncio, todo número do milestone está errado. E **salvar/retomar se separa**: cai do log quase de graça, mas é conforto e não medição, então entra como a cauda cortável.
- 2026-09-12: a sequência ficou em seis mais um desvio. A run como dado e o que ela mede; a quarta pergunta (a regra da Roster escrita, e a resposta que ela escolher); o que a mão faz (sink / segunda habilidade); o board se explica (barra de vida, hit feedback, sprites de tier, preview que diz a pergunta, paleta); o board como participante (tiles com propriedades, segundo spawn port); e A\* + maze, que é o único item da página que é outro jogo e a única coisa que justificaria um `v2` - palavra que `docs/` nunca escreveu. **Endless não é slot, é desvio**: se o verify do v1.8 disser que dois recordes leem como um jogo partido ao meio, ele fura a fila pelo texto do próprio roadmap, e antes dessa resposta não dá para agendá-lo.
- 2026-09-12: duas decisões de arrumação que valem mais que a ordem. **O trio de legibilidade foi partido de propósito** - relatório e atribuição de leak são *medição* e foram para o item 1, o preview que diz a pergunta é *ensino* e ficou no item 4 com a arte. E **hit feedback foi lido como legibilidade e não como juice**: a curva gastou dez waves com HP como eixo único de dificuldade e o jogador não consegue ver HP nenhum, o que confere no código - só existe `tower.flashMs` no muzzle flash e o glitch-scatter da morte.
- 2026-09-12: e **a sequência mora no [Idea Bank](../Idea%20Bank/Idea%20Bank.md) e não no [Roadmap](../Roadmap.md)**, que é o ponto onde este projeto já derrapou uma vez. O roadmap só tem milestone escolhida com data de escolha; encher ele de v1.9 a v2.0 especulativas seria prosa afirmando um futuro que ninguém escolheu. A nota diz isso de si mesma na última linha da seção: é sequência e não cronograma. Os bullets ranqueados foram marcados no lugar, como a página já faz com os promovidos, e a linha de abertura que dizia "otherwise unprioritized" deixou de mentir.
- 2026-09-12: **o v1.8 fica para 2026-09-13, retomando do passo 1** - `level2` no `map.ts`, a segunda entrada no `runs.ts`, e o gate que roda sobre *todos* os mapas declarados e não só sobre o novo. Nada foi escrito dele: o plano do [Roadmap](../Roadmap.md) está como foi fechado hoje de manhã, com os waypoints exatos deliberadamente fora dele, e a árvore não tem código nenhum pendente. A ordem de parar e ranquear primeiro foi do jogador, e o que ela comprou está escrito acima - a sequência que o banco não tinha, e a descoberta de que dois itens dela eram um.
- 2026-09-12: **v1.8 passos 1 a 4 numa sessão, e o passo 4 derrubou a premissa do próprio milestone.** O board novo é `(0,2) → (10,2) → (10,4) → (5,4) → (5,6) → (15,6)` - vai pela linha 2, **volta cinco colunas** pela linha 4, e sai pela linha 6. RECURSION 02, 16x9 como o mapa 1 e como o plano tinha fixado.
- 2026-09-12: o gate do mapa roda sobre o `levels` do `map.ts` e não sobre uma lista redigitada no teste, então **um terceiro board é gateado por existir**. Cinco regras gerais (dentro do grid, tudo em eixo, nunca pisa duas vezes no mesmo tile, spawn e core em bordas opostas, sobra board pra construir) e duas específicas: tem que existir uma perna que anda pra trás, e a contagem de tiles com traçado em cima *e* embaixo tem que dar dez. Essa contagem virou o número mais importante do milestone sem ninguém ter previsto.
- 2026-09-12: **o `runs.ts` virou dois e o `shell.ts` não foi tocado na parte que importa** - o `runs.length === 1 ? 'START' : run.name` do v1.7 continua exatamente como estava e só passou a cair no outro lado. É o que aquele passo prometeu e é raro isso se confirmar.
- 2026-09-12: o que mudou no picker foi tudo em volta: as duas linhas moram num `.shell-runs` com 10px entre elas dentro dos 22px da caixa, porque **duas linhas juntas leem como uma escolha com duas respostas e duas linhas a 22px leem como duas ofertas separadas.** E a pergunta que o passo tinha em aberto foi respondida com **nada pré-selecionado**: a lista lê igual toda vez, na ordem de declaração. Reordenar por histórico economizaria um toque e custaria o motivo de existir lista - o mesmo board no mesmo lugar toda vez.
- 2026-09-12: **o board entrou na chave do recorde, não no valor** - `ice-breaker:records:<id>`. Dois documentos, então write de um board não encosta no do outro, e as duas falhas ficam separadas: retune de curva derruba os dois conjuntos, board novo não tira nada do antigo. A adoção do conjunto velho é fallback de leitura e não migração: a chave sem board lê como MAINFRAME 01 até o primeiro recorde daquele board mudar o conjunto de lugar. A chave antiga fica onde está de propósito - service worker serve a build de ontem por um load depois do deploy, e é justo esse load que acharia o recorde sumido.
- 2026-09-12: e o picker forçou uma terceira coisa que não estava no passo: **com dois boards não existe "seu recorde"**, então o readout tem que dizer de qual board ele fala. Diz o último jogado (`ice-breaker:last-run`, escrito em toda run terminada, tenha batido recorde ou não), com o nome do board como cabeçalho sublinhado em cima das linhas. Jogar mal continua sendo ter jogado.
- 2026-09-12: **o harness ganhou o conceito de `Board`** - id, nome, level e os layouts escritos nos tiles dele - com os mesmos sete nomes declarados nos dois, pra `veteran` contra `veteran` ser afirmação sobre os boards com curva, roster e ordem de compra parados. Dois gates seguram as duas listas: os ids do harness têm que bater com os do `RUNS`, e todo layout tem que comprar a escada inteira contra uma curva que ele ganha - porque **tile que a regra recusa não enfraquece a run, ele trava a fila de compra atrás dele** e sai no relatório como layout que não se pagou. Essa armadilha me pegou três vezes durante a medição antes de virar teste.
- 2026-09-12: **a leitura: o RECURSION 02 limpa a curva sem tomar nada.** Primeiro sangue em 1.15x contra os 0.65x do mapa 1, core 5/5 contra 2/5. O suspeito óbvio era o traçado, 29 tiles contra 19 - que é exatamente o que o plano do milestone tinha escrito como o custo da dobra.
- 2026-09-12: **e o suspeito óbvio era inocente, o que é o achado do dia.** O controle é o mesmo board, as mesmas Cycles e a mesma ordem de compra com as armas movidas pra fora do corredor: essa run **perde o core a 1x e sangra em 0.65x**, o número exato do mapa 1 num traçado dez tiles mais longo. Traçado que nenhuma torre cobre não é tempo sob fogo, é inimigo andando de graça. **Comprimento não é dificuldade. Cobertura é.** A frase do roadmap ficou de pé com a correção embaixo, porque é contra ela que o milestone foi planejado.
- 2026-09-12: sobra a largura do corredor como única alavanca, e ela foi varrida: 2 tiles lê 0.87x, 4 lê ~1.0x, 6 lê ~1.1x, 10 lê ~1.2x. **Não existe largura que seja dobra de verdade e dificuldade de mapa 1 ao mesmo tempo** - estreita o bastante pra empatar é estreita o bastante pra deixar de ser o board. Quatro geometrias testadas antes de aceitar isso, incluindo pernas a três linhas de distância e uma versão com um corredor só; todas caíram na mesma conclusão.
- 2026-09-12: **decisão do jogador, com os quatro desenhos e os números lado a lado: corredor de dez.** E o certo aqui não foi a decisão, foi como a pergunta chegou nela - as três primeiras versões que eu escrevi falavam de "tiles" querendo dizer casas de traçado, e ele leu como tamanho de grid, com razão. Desenhar os 16x9 inteiros com régua, com `O` marcando as casas que atiram nas duas pistas, é o que fez a escolha ser escolhível. **Desenhe o layout antes de falar do layout** - a mesma lição de 25/08, num contexto totalmente diferente.
- 2026-09-12: com isso a afirmação honesta não é "o segundo board é mais fácil", é a mais afiada que o controle achou: **ele é o mais perdoado dos dois pra quem acha a dobra e o mais duro dos dois pra quem não acha.** Isso move o passo 5 de lugar - a pergunta deixa de ser se o corredor é forte, que o harness já respondeu, e passa a ser se ele é *visível*.
- 2026-09-12: o gate da banda da curva (`sangra abaixo de 1x, morre acima`) ficou escopado ao MAINFRAME 01, com o porquê escrito no teste: **é afirmação sobre a curva, e a curva é afinada contra o board pra qual foi escrita.** Rodar ele nos dois falharia, e o único jeito de fazer passar seria achatar o segundo board no primeiro. No board da dobra o que fica gateado é o achado: corredor dentro, limpa; corredor fora, perde, tendo comprado a mesma escada.
- 2026-09-12: 67 testes, `tests/map.test.ts` novo. O que falta do v1.8 é o passo 5, que é do jogador e é no celular.
- 2026-09-12: **v1.9 escolhida e planejada no mesmo dia, a pedido do jogador, do topo da sequência que o banco tinha ranqueado de manhã**: a run como dado. E o v1.8 é o que a tornou urgente em vez de só próxima - a leitura inteira daquele milestone saiu de um script que nunca dispara Overclock, nunca vende e nunca muda de ideia, contra um board cuja tese é uma decisão de posicionamento. A leitura valeu; o que ela não consegue dizer é o que uma *pessoa* faz ali.
- 2026-09-12: uma coisa checada antes de escrever o plano, e ela afinou o primeiro passo: **o loop do jogo não é onde as duas simulações poderiam ter divergido.** O `GameLoop` é acumulador de passo fixo e chama `update(TICK_MS)` sempre, igual ao harness. A costura de risco é o `hooks` - o burst de partículas é o único `Math.random()` do projeto e o jogo passa um hook que o harness não passa. O gate do passo 1 aponta pra lá, e não pro relógio.
- 2026-09-12: e o replay nasce escopado à curva **e** ao board, que é a terceira vez que essa lição é paga e a primeira em que ela sai de graça - `CURVE_ID` é do v1.7 e `RunDescriptor.id` é de hoje, os dois já querem dizer exatamente isso. Retune a curva ou troque o mapa e o log guardado passa a ser sobre outro jogo.
- 2026-09-12: não-objetivo com o motivo junto: **compartilhar replay, não.** Entregar uma run pra outra pessoa quer servidor, e backend é o único item que o banco insiste em chamar de outro projeto em vez de próximo passo - estático no Pages é o que faz o deploy ser um push e o jogo funcionar offline. Copiar o log como texto é o limite.
- 2026-09-12: **o board foi redesenhado depois de fechado, e a ideia foi do jogador olhando o desenho: spawn e core nos cantos, e o caminho usando as nove linhas.** Ele veio junto com uma objeção - que o mapa 2 não devia ter mais casas que o mapa 1 - e a objeção estava mirando no alvo certo pelo motivo errado. Mais casas não é o problema, e além disso não tem saída: o traçado do mapa 1 já é caminho mínimo, então dobra de `k` colunas custa `19 + 2k` e não existe dobra em 19.
- 2026-09-12: **mas os cantos consertaram, e por um motivo que ninguém tinha visto.** Chegar nas duas quinas força as três pernas a **4 linhas de distância** em vez de 2, e é essa a dificuldade inteira: torre a uma linha de uma pista cobre ~4,6 casas dela; torre a duas linhas de duas pistas cobre ~3 de cada. A dobra continua sendo o melhor imóvel do board, só que valendo ~30% em vez de ~100%. Com as pernas em 0/4/8 e a dobra puxada de cinco colunas para três: **sangra em 0.79x e morre em 1.25x**, contra os 0.65x e 1.14x do mapa 1, tomando um leak a 1x onde o mapa 1 toma três.
- 2026-09-12: e o redesenho virou a prova mais limpa do achado do dia. **O traçado tem 29 nos dois desenhos** - o de pernas a 2 linhas e o de pernas a 4. Mesmo comprimento, mesma curva, 1.15x contra 0.79x. Não sobra nenhuma outra variável: o que custa ao jogador é cobertura, e nunca foi comprimento.
- 2026-09-12: as duas tabelas do harness agora têm o mesmo formato, que é o teste que importa mais que qualquer margem isolada: **só o `veteran` limpa nos dois boards, e o `rush` fura nos dois.** Antes o RECURSION 02 tinha dois layouts limpando.
- 2026-09-12: **e o gate teve que aprender alcance em vez de vizinhança.** Com as pernas a 2 linhas, "cobre as duas" dava pra checar contando vizinhos - traçado exatamente uma linha acima e uma abaixo. A 4 linhas isso dá zero, e o teste honesto é o que uma *torre* enxerga, que é o que estava sendo medido desde o começo. Entrou um `REACH` no `map.ts`, que é o único ponto onde geometria e balanceamento se tocam, com o porquê escrito lá.
- 2026-09-12: mais um gate que nasceu da ideia: **cantos e nove linhas viraram teste.** Uma edição futura que puxasse as pernas pra dentro devolveria a força antiga pra dobra e nada mais no projeto perceberia - é exatamente a classe de regressão silenciosa que esse `docs/` existe pra não repetir.
- 2026-09-12: risco checado antes de aceitar, e ele é menor do que parecia: a barra de status e a engrenagem são ancoradas **à direita** do topo, e na linha 0 o traçado vai de x=0 a x=9, então a quina direita fica vazia. O painel de torre fica *embaixo* do board. Fica pro verify no celular confirmar, mas não é motivo pra recuar.
- 2026-09-12: **e eu errei a palavra três vezes antes de a pergunta ficar respondível.** Eu dizia "tiles" querendo dizer casas de traçado, e o jogador leu como tamanho de grid - com razão, porque eu também tinha cortado as linhas vazias do desenho e mostrado um 16x6. Desenhar os 16x9 inteiros, com régua e com `O` marcando as casas que atiram nas duas pistas, é o que fez a escolha ser escolhível **e** é o que produziu a ideia dos cantos. É a lição de 25/08 de novo: desenhe o layout antes de falar do layout.
- 2026-09-12: **e teve um terceiro desenho, também do jogador, depois de rodar o dev server e jogar de verdade: um S.** Entra e sai na metade das bordas, na mesma altura, e toda a volta acontece no eixo Y entre x=3 e x=12 - desce, sobe seis linhas de uma vez, desce de novo. O board ficou `(0,4) → (3,4) → (3,7) → (7,7) → (7,1) → (12,1) → (12,4) → (15,4)`.
- 2026-09-12: **o S não anda pra trás em x nenhuma vez** - a volta toda é vertical. Isso é o que o deixa mais barato que a dobra horizontal: coluna refeita se paga duas vezes, linha não. 27 de traçado contra os 29 das duas versões anteriores, e ainda assim o mais longo dos dois boards do jogo.
- 2026-09-12: e ele é **mais fácil que a versão dos cantos, não mais difícil**: 0.96x contra 0.79x, porque o S tem *mais* cobertura dupla e não menos. Cada cotovelo é um bolsão onde uma torre pega dois trechos perpendiculares, são dois cotovelos, e a espinha vertical longa é coberta quase inteira por uma torre do lado. Registrado como troca aceita com a tabela na tela, e não como coisa que passou batido: ele ainda sangra abaixo de 1x, que é o gate do projeto, e morre em 1.17x contra os 1.14x do mapa 1.
- 2026-09-12: **os três desenhos são o achado do milestone, juntos.** Dois deles tinham exatamente 29 de traçado e 0.36x de diferença; o que shipou é o mais curto dos três e caiu no meio. Não sobra nenhuma leitura em que comprimento seja a alavanca. O milestone saiu pra medir se a dobra custava traçado demais e descobriu que traçado nunca foi o preço.
- 2026-09-12: os gates foram reescritos pela segunda vez no dia, porque testavam a geometria da dobra. Agora o que é gateado é o que faz um S ser um S: **o traçado tem que inverter o sentido vertical pelo menos duas vezes** (o mapa 1 dá zero, então não passa por acaso), nunca pode andar pra trás em x, os cotovelos têm que enxergar traçado nos quatro lados, e a melhor casa daqui tem que cobrir mais que a melhor casa do mapa 1. Mais spawn e core na mesma altura em bordas opostas, que é o que faz tudo no meio ler como desvio.
- 2026-09-12: **e o controle do `balance.test.ts` virou regra em vez de lista escrita à mão** - cada arma vai pra casa mais fraca a no máximo 2 tiles de onde o `veteran` a colocou, sem repetir tile. Assim ele não pode ser acusado de ter sido desenhado pra perder, e a coleira curta mantém a linha plausível em vez de virar uma linha que abandonou o traçado. Resultado: perde o core sangrando em 0.64x, que é o número do mapa 1 num traçado 8 casas mais longo.
- 2026-09-12: e a lição do dia não é sobre geometria: **as duas correções vieram do jogador olhando desenho, não número.** A primeira veio de um ASCII de 16x9 com régua, a segunda veio de rodar o dev server e jogar. Eu tinha varrido quatro famílias de geometria com o harness e chegado em "não existe largura que sirva"; as duas saídas estavam fora do espaço que eu estava varrendo. O instrumento responde a pergunta que você faz e não a que você devia estar fazendo.
- 2026-09-12: **as linhas do picker viraram cards, a pedido do jogador com os dois boards já jogáveis**: miniatura do mapa, nome, e os recordes daquele board. Um card por entrada, e o card inteiro continua sendo um botão só - tudo dentro dele descreve a mesma coisa, que é a run que está prestes a começar, então não tem nada ali que queira um toque próprio.
- 2026-09-12: a miniatura é o `pixelSvg`, o mesmo builder do coração do HUD e da nota musical do botão de som, e não um caminho novo. **Um board *é* uma grade de pixels inteiros**; desenhar o mapa de qualquer outro jeito seria a única imagem do projeto que não é pixel art. O traçado vira grade de `#` e o run-length encoding transforma cada perna num `<rect>`, então um mapa inteiro custa uns sete elementos. Duas camadas: traçado apagado e as pontas acesas, porque a forma diz *qual* mapa e as pontas dizem pra que lado ele corre.
- 2026-09-12: 64px sobre um board de 16 de largura dá exatamente 4px por tile, e isso é **a regra de escala inteira do renderizador do board chegando na chrome pela terceira vez** - a primeira foi a nota musical do v1.5, a segunda o próprio board. Em escala fracionária o traçado de um tile rasterizaria com duas espessuras diferentes dependendo da perna.
- 2026-09-12: **e o card apagou um mecanismo em vez de acrescentar um.** A primeira resposta pra "de qual board é esse recorde" tinha sido um bloco compartilhado em cima da lista, com cabeçalho de nome, apontando pro último board jogado - o que exigia uma segunda chave de storage (`ice-breaker:last-run`), escrita em toda run terminada, mais um fallback pra id que não nomeia entrada nenhuma. Com o recorde dentro do card, a pergunta deixa de existir: melhor tempo no card do MAINFRAME 01 é o melhor tempo do MAINFRAME 01. Foi tudo deletado - bloco, cabeçalho, chave, fallback e a busca no boot.
- 2026-09-12: vale registrar exatamente assim: **o mecanismo não estava errado, era uma resposta correta pra uma pergunta que o layout ainda estava fazendo.** O conserto certo não era melhorá-lo, era parar de fazer a pergunta. Sobrou o `main.ts` entregando um conjunto por board no boot, e ele continua sendo o único módulo que sabe onde recorde mora.
- 2026-09-12: **e o harness achou um bug que o jogo carregava desde o v1: dava pra *vencer com o core zerado*.** O `stepGame` marca `lost` no instante em que o core chega a zero, e umas linhas abaixo marca `won` quando o spawner acabou e o board está limpo. Só que inimigo que chega no core também **despawna** - então o último inimigo da última wave levando o HP final satisfaz as duas condições no mesmo tick, e a checagem de vitória, sem guarda, sobrescrevia a derrota.
- 2026-09-12: **e não era cosmético.** O `recordRun` arquiva `cleared: status === 'won'`, então essa run levaria os recordes de FASTEST e CLEANEST depois de ter sido morta pela última coisa do board - justamente os dois recordes que existem pra dizer "você sobreviveu à curva inteira". Conserto de uma linha (`status === 'playing' &&`), com dois testes: o caso da corrida, e o oposto, pra guarda não transformar toda run terminada em derrota.
- 2026-09-12: o jeito como ele apareceu vale tanto quanto o bug: um `won 0/5` numa tabela do `npm run balance`, enquanto eu media um desenho alternativo do board 2. **Ele só é alcançável quando um board está afinado perto o bastante da curva pro último inimigo ser o que te mata** - por isso nunca apareceu jogando, e por isso o instrumento achou antes de qualquer pessoa. De brinde, o `morre` do RECURSION 02 caiu de 1.17x pra 1.15x: ele estava ganhando uma vitória fantasma em algum multiplicador do bisect.
- 2026-09-12: **o jogador achou uma assimetria de 1 tile no S, e ela não tinha conserto completo - só tinha lugar.** O board tem 16 colunas, ou seja 15 passos de borda a borda, que é ímpar; um S simétrico precisa de duas aproximações iguais mais duas barras iguais, que é par. A espinha teria que ficar em x=7,5. **Um par sempre fica 1 fora**, e a única decisão disponível é qual.
- 2026-09-12: e ela estava no pior par possível: **as duas barras**, que ficam uma em cima da outra e são as mais fáceis de comparar na tela. O board virou `(0,4) → (3,4) → (3,7) → (7,7) → (7,1) → (11,1) → (11,4) → (15,4)` - barras 4 e 4, cotovelos 4 e 4, verticais curtas 3 e 3, e o tile ímpar foi pras aproximações: 3 de entrada contra 4 de saída, a onze colunas de distância, uma com a porta de spawn desenhada e a outra terminando no core. Ninguém lê aquilo como par.
- 2026-09-12: **a primeira tentativa de arrumar custou a melhor casa do board e o gate pegou.** Eu tinha puxado a entrada de x=3 pra x=2 pra igualar as barras em 5 - o que afastou as duas verticais além do alcance de torre, e a melhor casa caiu de 9 pra 7, o número do mapa 1. Ou seja: o board teria ficado simétrico e deixado de ser o board. **É exatamente por isso que aquele gate compara com o mapa 1 em vez de afirmar uma constante** - constante eu teria atualizado sem pensar.
- 2026-09-12: a versão que ficou mede 0.96x pro primeiro sangue e **1.14x pra morte, que é o número do mapa 1 na vírgula**. Dificuldade igual à de antes da arrumação, e agora com os dois cotovelos iguais - o que também quer dizer que as duas metades do board fazem a mesma pergunta com material diferente.
- 2026-09-12: **o board 2 foi fechado pelo jogador, depois de três desenhos e uma arrumação.** Vale registrar a sequência porque nenhuma das correções saiu do harness: dobra horizontal de pernas a 2 linhas (1.15x, limpava sem tomar nada) → cantos com pernas a 4 linhas (0.79x) → S com a volta no eixo Y (0.96x) → tile ímpar movido das barras pras aproximações. Eu tinha varrido quatro famílias de geometria com o instrumento e concluído "não existe largura que sirva"; as duas saídas estavam fora do espaço que eu estava varrendo, e as duas vieram de olhar desenho e de jogar.
- 2026-09-12: com isso o v1.8 deve só o passo 5, e ele encolheu: **as perguntas sobre o board já foram respondidas no desktop** - a dobra lê como segunda pergunta, o cotovelo se acha sozinho o bastante pra render três redesenhos. O que sobra é celular, e duas coisas novas que não existiam quando o passo foi escrito: se os cards com miniatura e recorde cabem numa tela baixa, e se dois recordes leem como dois jogos ou como um jogo partido ao meio - que é a única das três que ainda pode furar a fila do v1.9 com o endless.
- 2026-09-12: **v1.8 fechado com um desvio anotado: o verify foi no desktop e não no celular.** O hábito "todo milestone verifica no celular" é do v1.2 e foi quebrado aqui. Motivo achado e não adivinhado: o dev server respondia da própria máquina e não do celular porque **o perfil desta rede no Windows está como `Public`**, e perfil público recusa conexão de entrada. Diagnosticado com `Get-NetTCPConnection` mais um GET em IPv4 que voltou 200 - ou seja, nem era o Vite nem era binding. Não consertei: é regra de firewall na máquina do jogador, decisão dele e não deste milestone.
- 2026-09-12: e o que o desktop respondeu ele respondeu três vezes: **um verify que produz três redesenhos fez mais que um verify que produz um sim.** As perguntas sobre o board estão fechadas por terem sido jogadas, não por terem sido feitas.
- 2026-09-12: o que continua devido ao celular encolheu sozinho. **O risco que o passo 5 existia pra pegar - traçado embaixo da chrome da banda de cima - evaporou com o S**, que usa as linhas 1 a 7 e deixa a 0 e a 8 vazias. Sobra retrato, onde o board gira um quarto de volta e ninguém leu o S deitado; a altura de dois cards de 56px com título e rodapé numa tela baixa; e os alvos de toque dentro de um card que agora tem miniatura e texto no mesmo botão.
- 2026-09-12: **e a terceira pergunta não foi respondida de jeito nenhum** - se dois recordes leem como dois jogos ou como um jogo partido ao meio. Ela precisa de duas runs terminadas e de uma olhada na shell depois, e a sessão de desktop não fez isso. Fica como *não contrariada* e não como confirmada, que é a mesma distinção que o verify do v1.7 teve que fazer. É a única daqui que ainda pode desviar o que vem: "partido ao meio" manda o endless pra frente da fila pelo texto do próprio roadmap.
- 2026-09-12: **v1.9 passo 1 - o gate de determinismo, e a resposta é sim: o jogo e o harness são a mesma simulação.** É a resposta que o milestone precisava e não a mais interessante das duas; se tivesse voltado "não", todo número que o v1.3, o v1.6 e o v1.8 imprimiram seria sobre um jogo que ninguém joga.
- 2026-09-12: e o achado estrutural do passo: **o `game-loop.ts` não tinha teste nenhum.** O módulo por onde passa todo tick jogado era o único da simulação sem gate - justamente o que separa o caminho do jogador do caminho do instrumento. `requestAnimationFrame` e `performance.now()` stubados com relógio na mão, mesmo idioma do `localStorage` do `records.test.ts`, e isso compra mais que portabilidade: **relógio na mão serve frame de 3ms e de 92ms, que é onde um passo fixo se sustenta ou não.**
- 2026-09-12: seis gates. Cinco sobre o loop - todo tick é `TICK_MS`, o resto é carregado (frame irregular e frame parelho do mesmo total rodam a mesma simulação), travada de 60s é cortada em 250ms em vez de recuperada, **velocidade escala quantos ticks rodam e nunca o tamanho de um**, e pausa não guarda tempo pra gastar na volta. O do 2x é o que sustenta o recorde de tempo existir.
- 2026-09-12: o sexto é o gate: um estado dirigido pelo `GameLoop` com frames irregulares e com os hooks do `main.ts`, outro por um `for` de `stepGame` sem hook nenhum, e **o estado inteiro comparado** depois do mesmo número de ticks - não os quatro escalares que o teste de hook do v1.3 se contentou em olhar. Partículas são a única exceção, e ela é verificada em vez de suposta: a chamada do hook é contada, então uma comparação que passa não pode ser uma em que nada aconteceu.
- 2026-09-12: duas asserções minhas nasceram erradas e as duas valem anotar. `Math.floor(250 / TICK_MS)` dá 14 e não 15, porque 250/16,666… fecha em 14,999999999999998 - eu estaria gateando um artefato de float em vez do clamp. E exigir que 2x rodasse exatamente o dobro de ticks é **especificar demais**: isso afirma onde o resto acumulado caiu, que é propriedade daqueles frames e não do loop.
- 2026-09-12: e uma terceira que era frágil em vez de errada: eu ia provar que o hook rodou olhando `particles.length > 0` no fim da run. **Partícula expira** - uma run longa o bastante pra valer medição é uma run que pode terminar sem nenhuma na tela. Contar a chamada do hook afirma a coisa certa, que é a costura ter sido cruzada, e não que a última travessia ainda esteja visível.
- 2026-09-12: **dívida achada de passagem, e ela é o assunto deste milestone um andar acima**: o `tsconfig.json` tem `"include": ["src"]`, então `tests/` e `scripts/` nunca passam pelo `tsc`. Nada faz um teste concordar com a fonte que ele testa - foi assim que um `createEnemy('worm', 1)` (um `WaveScale` recebendo número) rodou nesta sessão e falhou como asserção errada em vez de como erro de tipo. Não consertei: quer `@types/node` num projeto que se manteve em três devDependencies, e isso é decisão de stack e não passo de milestone.
- 2026-09-12: **a sessão para aqui, no v1.9 passo 2, e nada dele foi escrito.** A árvore está limpa, 77 testes passam, `npm run build` fecha. O que retoma é *a run como log* - gravar o que o jogador fez como ticks e ações - e ele começa com uma decisão que o plano deixou explicitamente em aberto: **se o log é escrito continuamente ou montado no fim**, que é a mesma pergunta que o salvar/retomar faz pelo outro lado, e se vale guardar log quando ninguém pediu um.
- 2026-09-12: três coisas ficam em aberto e nenhuma delas é código. **A pergunta do celular que pode desviar o milestone**: dois recordes leem como dois jogos ou como um jogo partido ao meio? Precisa de duas runs terminadas e de uma olhada na shell depois; se voltar "partido", o endless fura a fila e vira a v1.9 no lugar desta. **A dívida do `tsconfig`**: `include` só tem `src`, então `tests/` e `scripts/` nunca são type-checkados, e consertar quer `@types/node` num projeto de três devDependencies - decisão de stack, do jogador. **E a regra de firewall**: o perfil desta rede é `Public`, o celular não alcança o dev server, e a regra estreita (porta 5173, só sub-rede local) precisa de terminal elevado.
- 2026-09-12: e uma coisa pra *não* refazer. **Comprimento de traçado não é dificuldade, e isso está medido de três jeitos** - o controle de torres dentro e fora do corredor no mesmo board, dois desenhos de 29 tiles com 0.36x de diferença, e o desenho final que é o mais curto dos três e caiu no meio. Se uma sessão futura for tentada a varrer geometria de novo, a alavanca é cobertura: quanto de traçado um orçamento de torres alcança ao mesmo tempo. O gate disso mora no `balance.test.ts` e compara contra o mapa 1 em vez de afirmar constante, **de propósito** - constante alguém atualiza sem pensar, e foi exatamente isso que pegou a arrumação de simetria que teria custado a melhor casa do board.
- 2026-09-12: **v1.9 passo 2 - o log é escrito continuamente, e o que é montado no fim é só o envelope.** A alternativa era reconstruir o log do estado final, e ela caiu pelo mesmo motivo que apagou a chave `last-run` no v1.8: um segundo relato do que aconteceu é uma segunda coisa que pode estar errada sobre isso. Toda ação já é uma função no `game.ts` que devolve se pegou, então gravar é uma linha no ponto do fato. Board, curva e comprimento são selados no fim porque são fatos *sobre* a run e não eventos *dentro* dela.
- 2026-09-12: **quem grava é a ação e não quem chama, porque só a ação sabe se aconteceu.** Um toque em tile ocupado e um toque sem Cycles chegam os dois no `placeTower`, e nenhum dos dois é evento da run. Logar de fora arquivaria a *intenção*, e o replay estaria reproduzindo uma run que ninguém jogou. O gate disso é o teste das recusas: seis taps negados, log intacto.
- 2026-09-12: **o tick virou estado, e isso apagou dois contadores.** `GameState.tick` é o relógio que as duas simulações compartilham - e o `runMs` do `main.ts` (que somava um `dtMs` sempre igual a `TICK_MS`) e o `ticks` local do `runBalance` eram o mesmo número mantido à mão em mais dois lugares. O recorde de tempo agora é `run.tick * TICK_MS` lido da própria run, e o `durationMs` do harness também. Um contador só, e é o que carimba cada linha do log.
- 2026-09-12: **o Overclock ganhou uma porta que conhece a run, e é a que o passo 4 estava esperando.** `triggerOverclock` recebe uma torre e não sabe de run nenhuma, o que estava certo enquanto o único caller era um tap. Mas é ação de jogador, então é ação que o replay tem que reproduzir: `overclockTower(state, tower)` loga quando dispara. De brinde, o caveat que aparece embaixo de toda tabela de margem desde o v1.6 - nenhuma leitura jamais usou Overclock - deixa de ser falta de ponto de entrada.
- 2026-09-12: **guardar quando ninguém pediu: sim enquanto joga, não depois.** Gravar é o que deixa o log disponível pra quem finalmente perguntar, e uma run inteira são dezenas de entradas. Persistir é outra decisão, com perguntas próprias - quais runs, quantas, o que acontece com elas quando a curva anda -, e nada perguntou ainda. O log é selado no tick em que a run acaba, mesmo lugar onde o recorde é arquivado e pelo mesmo motivo, e fica na memória. Hoje quem lê é um `runLog()` de DEV; quem lê depois é o passo 5.
- 2026-09-12: o formato é texto e não JSON: **uma linha por ação, verbo na frente**, porque o valor inteiro desse artefato é caber num bug report digitado no celular - e JSON gastaria a maior parte dos bytes repetindo as mesmas cinco chaves. O parser é estrito de propósito (cabeçalho, verbo, tipo de torre, tile inteiro, ordem dos ticks) e devolve `null` em vez de log parcial: um log que lê com uma linha a menos vira outra run e reporta a diferença como bug de determinismo.
- 2026-09-12: e o `ticks` do envelope existe por uma razão que eu quase não vi: **as ações não implicam o comprimento da run.** A última wave é andada e morta sem ninguém apertar nada, então um replay que parasse na última ação logada pararia antes justamente do veredito que ele existe pra reproduzir.
- 2026-09-12: de graça, vindo do passo 1: **o gate de determinismo agora compara o log também**, porque ele compara o estado inteiro e o log mora no estado. Duas runs que concordam em tudo e discordam ali não são a mesma run. Não custou uma linha. 84 testes passam, `npm run build` fecha, e a tabela do `npm run balance` saiu **idêntica byte a byte** à do commit anterior - conferida com `diff` num worktree e não no olho, porque "era pra dar igual" é exatamente o tipo de coisa que este projeto já pagou pra não supor.
- 2026-09-12: **a primeira run de verdade foi jogada, e ela mexeu no formato.** Vinte e seis ações no RECURSION 02 em 2:31, e três coisas saíram dali que nenhum log sintético teria dito. A primeira: a última ação é no tick 8141 de 9081 - **quinze segundos de run depois do último toque**, com a última wave andada e morta sem ninguém apertar nada. É exatamente o buraco que o campo `ticks` do envelope existe pra tapar, agora confirmado em vez de raciocinado.
- 2026-09-12: a segunda: **zero sells e zero Overclocks numa run inteira.** Duas mecânicas que o jogo tem e que uma run completa não usou. Uma run não é amostra, mas é a primeira vez que este projeto tem *como ver* isso - e cai bem em cima do passo 4, que ia medir quanto vale o Overclock numa run que não o usou.
- 2026-09-12: e a terceira, que mudou código: **o envelope não carregava o veredito.** Um log gravado aqui dentro pode ser conferido contra o estado vivo na memória; o caso que o milestone *nomeou* - log colado de um celular que não está aqui - não tem com o que discordar. **Replay que nada pode falsificar não é gate.** Achado olhando um log real, não escrevendo o parser.
- 2026-09-12: **o log ganhou a metade de saída, e quem completou foi o jogador: gravar os inimigos que passaram.** Formato virou `ice-breaker/2`. A separação que isso deixa clara é a que importa: **ação é entrada** - é o que o replay *faz*; **vazamento e fim são saída** - é contra o que ele é *conferido*. E quanto mais fina essa metade, mais o gate pega: um replay que chega no mesmo core HP vazando outra wave passa no veredito e falha aqui.
- 2026-09-12: **vazamento é gravado como inimigo e não como ponto de vida**, que é a única coisa que só a simulação sabe dizer no instante em que acontece. Todo outro contador de leak do projeto - o recorde, as tabelas do harness - é HP perdido do core, e essa definição fica; mas não é o mesmo número, porque um Zero-Day leva três sozinho. "Cinco leaks" pode ser cinco worms ou duas coisas e um boss, e até hoje não havia onde perguntar qual.
- 2026-09-12: a linha de fim carrega status, core HP, Cycles e wave, e **o tick dela é o comprimento da run** - então o cabeçalho voltou a ser só identidade (board e curva). As duas metades são impressas num timeline só, com o leak na frente da ação quando dividem o tick: não é critério de desempate, é o que aconteceu - leak acontece *durante* o tick e ação acontece depois dele, porque não dá pra tocar na tela no meio de um passo.
- 2026-09-12: o `GameState.log` virou `GameState.actions`, com `leaks` do lado. Nome de um commit de vida, trocado porque a distinção entrada/saída passou a ser o argumento do módulo inteiro - e `log` guardando só metade do log seria a próxima pessoa lendo errado.
- 2026-09-12: e isso **não é o relatório do passo 5 chegando cedo.** O log *grava* a atribuição de leak; o que o passo 5 deve é um jogador lendo ela. Alimenta aquele passo em vez de furar a fila dele. 86 testes passam, `tsc` limpo, build fecha, e a tabela do `npm run balance` de novo idêntica byte a byte à do commit anterior - conferida com `diff`.
- 2026-09-12: **a segunda run voltou limpa - `won 5 64 15` em 2:08, sem uma linha de `leak`** - e ela é o achado mais forte que este projeto já tirou de uma partida. Guardada em `tests/runs/recursion-02-flawless.run` - extensão `.run` e não `.log`, porque `*.log` é ignorado pelo git desde o template do Vite, onde "log" quer dizer diagnóstico e não partida. Guardada porque é a única run humana que existe e não dá pra regerar, e porque o passo 3 pede exatamente isso: uma run gravada replayando pra um `RunReport` idêntico.
- 2026-09-12: **o jogador ganhou o RECURSION 02 com oito torres dentro de um bloco 3x3, e o `veteran` do harness espalha pelo board.** As tiles dele são (4,5) (5,5) (6,5) (4,6) (5,6) (6,6) (4,7) (5,7) - tudo em x 4-6, y 5-7, com sete upgrades em cima. O `VETERAN_BUILDS_2` abre no cotovelo perto e **muda pro cotovelo longe na wave 9**, com o argumento escrito no próprio arquivo: "a metade da run que a abertura não alcança". A run humana nunca foi lá, e não vazou nada.
- 2026-09-12: e a leitura é sobre o board, não sobre o jogador: **o S dobra, então um bloco no meio cobre as duas passagens.** É literalmente a tese que este log escreveu quando o board fechou - "as duas metades do board fazem a mesma pergunta com material diferente" - e o layout que o harness declara pra ele ignora a dobra e espalha assim mesmo. A alavanca era cobertura, que é o que estava escrito na nota de "não refazer"; o que o instrumento continuava declarando era alcance de traçado.
- 2026-09-12: os números lado a lado, mesma curva e mesmo board: `veteran` fecha em 3:18 com 4 de core, 1 leak e 62 banked; a run humana fecha em **2:08 com 5 de core, 0 leaks e 64 banked**. Setenta segundos e um core de vantagem, com dois Cycles a mais no bolso. Parte do tempo são os **14 early calls**: é o `rush` sendo agressivo com a sobrevivência do `veteran`, num layout que nenhum dos dois declara.
- 2026-09-12: **e de novo zero sells e zero Overclocks.** Duas runs, duas mecânicas intocadas. Uma pessoa não é amostra, mas o passo 4 - "quanto valeu o Overclock na run que foi jogada de verdade" - já tem uma resposta desconfortável esperando por ele: nas duas runs que existem, zero.
- 2026-09-12: o que **não** dá pra afirmar ainda, e vale escrever antes de alguém citar isso como medido: nada disso passou por replay, porque replay é o passo 3. Os três números do fim são comparáveis com o harness porque são a mesma definição, mas a tabela wave a wave que o passo 6 quer só existe depois que a run humana rodar dentro do `balance.ts`. Também não declarei um loadout `player` no harness com essas tiles: seriam as tiles dele sem ser a run dele - sem a ordem dos upgrades e sem os early calls -, e isso é aproximar de novo aquilo que este milestone existe pra parar de aproximar.
- 2026-09-12: **v1.9 passo 3 - a run do jogador replayou com zero divergências.** `won 5 64 15` em 2:08, reproduzido a partir de vinte e nove linhas de texto. É a afirmação do milestone inteiro caindo numa partida de verdade em vez de em dois caminhos de código: o `game-loop.test.ts` só conseguia afirmar que o loop do jogo e o loop do harness concordam; isto afirma que **a run de uma pessoa e a leitura do instrumento são a mesma run**.
- 2026-09-12: **o replay passa por dentro do harness e não ao lado dele.** O `runBalance` entregou o laço pro `runSimulation(driver, options)`, e um `RunDriver` responde a única coisa que de fato difere: o que o jogador faz neste tick. Layout declarado gasta uma ordem de compra; run gravada toca de novo o que alguém fez. Escrever o laço duas vezes seria dois instrumentos tendo que concordar sobre uma leitura - a forma que este projeto vive deletando -, e assim uma run replayada e um layout declarado saem como o mesmo `RunReport`, lado a lado. O passo 6 acabou de ficar de graça. Refactor neutro: a tabela do `npm run balance` saiu byte a byte igual, conferida com `diff` contra o commit anterior.
- 2026-09-12: **a comparação é de propósito mais que o veredito, e tem um tamper que é o argumento inteiro.** Tira o último upgrade da run limpa e **o veredito não se move** - continua vitória com o core intacto -, mas os Cycles se movem (94 sobrando em vez de 64) e o relógio também (7721 ticks em vez de 7690). Um gate que comparasse só o fim chamaria isso de mesma run. Então o que é comparado é: as ações que o replay conseguiu tomar, os inimigos que passaram, e aí o fim.
- 2026-09-12: e a metade de entrada saiu de graça: **o replay grava as próprias ações pelas mesmas linhas do `game.ts` que gravaram a original.** Então uma ação que o replay não conseguiu pagar fica *faltando* na lista dele, e não apenas sem efeito. A máquina de gravar log virou o gate do próprio playback sem custar uma linha.
- 2026-09-12: **e uma coisa que eu ia gatear e não gateei.** Um placement movido um segundo pra frente dentro da calmaria de abertura é genuinamente a mesma run - zero divergências, porque ainda não tem nada no alcance. Isso é acidente de onde as waves calham de estar, não propriedade pra travar num teste; fica escrito aqui em vez de afirmado. O tamper que ficou move uma torre **um tile**, que é a tese do próprio board virada teste - e ele custa dano cedo o bastante pra uma compra lá na frente não ser mais paga, então a run para de bater na ação que o jogador gravado fez e este não conseguiu.
- 2026-09-12: uma run roteirizada cobre **sell e Overclock**, porque duas runs humanas já não tocaram em nenhum dos dois, e um replay que pulasse os dois em silêncio ainda reproduziria aqueles logs perfeitamente. As duas recusas do envelope moram no `replay.ts` e não no parser: outra curva, e um board que este build não tem - "não é log" e "é log de outro jogo" são respostas diferentes.
- 2026-09-12: 91 testes, `tsc` limpo, build fecha. O que o v1.9 ainda deve: Overclock no harness (passo 4), o relatório pós-run com atribuição de leak (5), a leitura contra o `veteran` (6) e o salvar/retomar (7, cortável). O passo 6 agora é ler um número que já dá pra imprimir.
- 2026-09-12: **v1.9 passo 6 - a run humana foi lida contra o harness, e o sink de economia não fura a fila.** `npm run balance -- --run <caminho>` replaya uma run gravada e imprime ela na mesma tabela dos layouts declarados no board dela, medida pelo mesmo laço - que é o que a extração do driver no passo 3 comprou. No RECURSION 02: `veteran` 4 de core, 1 leak, 422 ganhos, 62 no banco, 3:18; `rush` core arrombado, 439 ganhos, 79 no banco, 1:04; **a run jogada: 5 de core, 0 leaks, 494 ganhos, 64 no banco, 2:08.**
- 2026-09-12: **o limiar escrito no banco de ideias já não existia.** A condição era "se run real terminar com mais sobra que os 164 do `veteran`", e 164 é leitura da curva de dez waves - o v1.6 já tinha registrado esse número virando 62 e a sequência nunca foi atualizada. Contra a curva que existe: 64 contra 62. Isso é empate e não "muito mais".
- 2026-09-12: **e a forma diz mais alto que o total.** A run jogada desce a 5 Cycles na wave 6 e gasta 89% de tudo que passa pela mão dela; o `veteran` bate 0 na wave 5 e 2 na wave 14. Nenhum dos dois está sentado em dinheiro. E lendo a tabela inteira a correlação vai pro outro lado: quem banca mais é `starter` com 196, `spread` com 136 e `rush` com 79, e os três perdem. **Sobra mede principalmente quando a run parou de conseguir gastar** - a premissa do sink era que a run vencedora passa da metade com dinheiro que não usa, e a única run humana vencedora que existe não faz isso.
- 2026-09-12: então a sequência fica de pé: **a próxima é a quarta pergunta** (a regra da Roster escrita, e a resposta que ela escolhe). A única coisa que ainda pode furar isso é o endless, e ele depende da pergunta de celular que o v1.8 deixou em aberto - não desta leitura.
- 2026-09-12: **primeira leitura de brinde: toda a renda extra do jogador é a chamada adiantada.** 494 ganhos contra 422 do `veteran`, e 68 dos 72 de diferença são dinheiro de early call. Renda de kill é propriedade da curva e não do jogador - as duas runs matam quase os mesmos inimigos. A chamada é a única alavanca econômica que o jogador de fato tem.
- 2026-09-12: **segunda, e é a que aponta pra uma falta no instrumento: a chamada é um dial e o harness só gira até o fim.** O `rush` é o `veteran` mais `callWavesEarly`: pega 117 de bônus e arromba o core em 1:04. O humano pega **68 - uns 58% - e ganha com o core intacto em 2:08**, porque chama com uns cinco segundos sobrando em vez de no instante em que o port acende, comprando de volta uns três segundos de tempo de build por wave. A leitura antiga do `rush` - pegar toda chamada apaga o espaço pra estar errado - continua de pé e ganha a metade que faltava: **`callWavesEarly: boolean` não consegue expressar a posição onde gente de verdade joga.** Arquivado e não construído: o passo era leitura e não botão.
- 2026-09-12: **v1.9 passo 4 - o Overclock entrou no harness, e ele não confirmou o caveat: refutou.** `withOverclock` é um decorador de `RunDriver`, então a política se aplica igual a um layout declarado e a uma run humana gravada, e `--overclock` pareia cada linha com a mesma run apertando Overclock assim que sai do cooldown.
- 2026-09-12: **no RECURSION 02 isso custa quase toda a margem do `veteran` e salva o `rush` inteiro** - e os dois são *o mesmo layout comprando as mesmas coisas*: ambos gastam 460 de 545, então a habilidade é a única diferença. O `veteran` vai de 4 de core e primeiro leak em 0.96x para 1 de core e primeiro leak em **0.35x**. O `rush` vai de core arrombado para vitória com 3 de core, e de morrer em 0.82x para morrer em 1.17x - **melhor que o `veteran` sem Overclock**.
- 2026-09-12: então a frase impressa embaixo de toda tabela de margem desde o v1.6 - *"every row is a floor, not a measurement: the harness never fires Overclock"* - estava **errada e não só incompleta**. Ela supunha que a habilidade só podia somar. As linhas nunca foram piso; eram uma política entre várias. O texto foi trocado pelo que foi medido: **o par é um intervalo, e ele nem sequer é ordenado.**
- 2026-09-12: e o mecanismo está nos números, não numa inferência: o boost são 4 segundos a duas vezes e meia a cadência, comprados com 3 segundos sem atirar. Isso é +43% na média, com um buraco dentro. **Média é o que importa quando você está atrás, consistência é o que importa quando está na frente** - o `rush` está se afogando e aceita a troca, o `veteran` está confortável e não pode pagar o buraco.
- 2026-09-12: **e a run que o jogador jogou é a terceira resposta.** Oitenta apertos colocados por cima das decisões gravadas dele mudam o resultado em **nada** - mesma vitória, mesmo core intacto, mesmos 64 no banco - e tiram 1,8 segundo de uma run de 2:08. O que responde a pergunta que este passo existia pra fazer, na única forma que sempre foi honesta: não quanto vale o Overclock em abstrato, mas quanto teria valido na run que foi jogada. Nela, dois segundos.
- 2026-09-12: ou seja: **"eu não gosto de usar Overclock" não é buraco no jogo do jogador.** É a leitura certa de um board em que ele está na frente, e agora o instrumento diz isso com número. O que sobra é pergunta de design e não de tuning, arquivada e não respondida aqui: uma habilidade cujo valor inteiro é virada de jogo é uma habilidade que um jogador ganhando deveria ignorar - e esta é apresentada como botão permanente em toda torre de ataque, em vez de como algo que o board oferece quando está dando errado.
- 2026-09-12: o gate novo (`tests/overclock.test.ts`) trava o **mecanismo e não a leitura**: que o harness consegue disparar, que a linha se chama `+oc`, que o gasto é idêntico e que alguma outra coisa se moveu; e que os apertos da política caem no log da run como os de um jogador, porque um contrafactual é uma run e o log de uma run tem que replayar. A leitura em si é sharp demais pra virar constante que um retune falsifica em silêncio - ela mora aqui.
- 2026-09-12: 93 testes, `tsc` limpo, build fecha. A tabela default saiu igual até a largura da coluna do layout, que cresceu de 9 pra 12 caracteres pra caber `veteran+oc` - conferido com `diff` ignorando espaço.
- 2026-09-12: **v1.9 passo 5 - o relatório pós-run existe, e o que atravessou de propósito não foi a tabela do harness.** Jogador não está afinando curva, e as colunas que respondem "a curva está certa" não são as que respondem "o que eu construo da próxima vez". Então a shell imprime o veredito com o nome do board, o relógio, o core, quão longe uma run perdida chegou - e **o que passou, agrupado pela wave que levou o dano**.
- 2026-09-12: **agrupado por wave e não uma linha por chegada**, porque cinco linhas escrito WORM é uma lista e "a wave 10 deixou passar três worms" é uma leitura - e o relatório existe pra mudar o que se constrói, não pra recontar o que aconteceu. Contado **por inimigo e não por HP de core**, que é a distinção que só o log consegue fazer e o motivo de o log guardar inimigo: um Zero-Day é uma coisa pelo portão e três de vida do core. O recorde continua contando HP, como desde o v1.3 - os dois números divergem de propósito, e esta é a primeira tela em que os dois aparecem na mesma sessão.
- 2026-09-12: **o leak passou a carregar a wave dele, então o formato foi pra `ice-breaker/3`.** A wave de um leak só é sabível no instante em que ele acontece: waves se sobrepõem, então a wave que spawnou o inimigo e a wave que estava rodando quando ele chegou são afirmações diferentes - e a que o harness cobra desde o v1.3 é a segunda. Derivar depois significaria replayar a run pra ler um relatório, ou calcular duas vezes e torcer pras duas concordarem.
- 2026-09-12: **o relatório fica acima da lista, que é exatamente a forma que o v1.8 deletou.** Ele está certo aqui pelo motivo de estar errado lá: recorde é por board, então bloco compartilhado obrigava a shell a decidir de quem, e isso custou uma chave de storage pra responder. Relatório de run é sobre uma run que acabou de acontecer - ele nomeia o próprio board, não decide nada, não guarda nada, e morre junto com a tela do mesmo jeito que as marcas `NEW`. O resto do argumento é ordem de leitura: o título diz onde você está, o relatório diz o que aconteceu, a lista diz o que fazer agora.
- 2026-09-12: gateado onde dá: o `groupLeaks` é puro e exportado, porque o agrupamento é a única parte dessa tela que pode estar errada de um jeito que olhar não pega. Pra importar `shell.ts` no runner foi preciso declarar `__APP_VERSION__` antes do import - mesma manobra do stub de `localStorage` do `records.test.ts`, e pelo mesmo motivo: o módulo é importado por uma função pura e não deveria precisar de bundler pra ser alcançável.
- 2026-09-12: **a tela em si está devendo um olhar no celular** - e junto com ela a pergunta que o v1.8 deixou em aberto sobre dois recordes lerem como dois jogos ou como um jogo partido ao meio. Agora com um relatório em cima deles, que é altura nova numa tela que já era a apertada. 99 testes, `tsc` limpo, build fecha.
